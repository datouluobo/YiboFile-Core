<UserControl x:Class="OoiMRR.RightPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="100">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0">
        <Grid.RowDefinitions>
            <!-- 行0：标题栏区域（与列2工具栏对齐） -->
            <RowDefinition Height="Auto"/>
            <!-- 行1：预览区 -->
            <RowDefinition Height="*"/>
            <!-- 行2：分割器 -->
            <RowDefinition Height="6"/>
            <!-- 行3：备注区 -->
            <RowDefinition Height="200" MinHeight="120"/>
        </Grid.RowDefinitions>
        
        <!-- 行0：标题栏（占位，与列2工具栏对齐） -->
        <Border Grid.Row="0" Height="36" Background="Transparent"/>
        
        <!-- 水平分割器 -->
        <GridSplitter Grid.Row="2" Style="{StaticResource HorizontalGridSplitterStyle}"
                      HorizontalAlignment="Stretch"/>
        
        <!-- 行1：预览区 -->
        <Grid Name="PreviewGrid" Grid.Row="1" MinHeight="200">
            <TextBlock Text="选择一个文件进行预览" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Foreground="Gray" FontSize="14"/>
        </Grid>
        
        <!-- 行3：备注区 -->
        <TextBox Grid.Row="3" Name="NotesTextBox" Margin="8,8,8,8" 
                 AcceptsReturn="True" 
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 TextChanged="NotesTextBox_TextChanged"
                 Background="#FFFFFF" BorderBrush="#E9ECEF" BorderThickness="1"/>
    </Grid>
</UserControl>
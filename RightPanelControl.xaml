<UserControl x:Class="OoiMRR.RightPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="100">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0">
        <Grid.RowDefinitions>
            <!-- 行0：标题栏区域（与列2工具栏对齐） -->
            <RowDefinition Height="Auto"/>
            <!-- 行1：预览区 -->
            <RowDefinition Height="*"/>
            <!-- 行2：分割器 -->
            <RowDefinition Height="6"/>
            <!-- 行3：备注区 -->
            <RowDefinition Height="200" MinHeight="120"/>
        </Grid.RowDefinitions>
        
        <!-- 行0：标题栏（占位，与列2工具栏对齐） -->
        <Border Grid.Row="0" Height="36" Background="Transparent" IsHitTestVisible="False"/>
        
        <!-- 水平分割器 -->
        <GridSplitter Grid.Row="2" Style="{StaticResource HorizontalGridSplitterStyle}"
                      HorizontalAlignment="Stretch"
                      DragCompleted="GridSplitter_DragCompleted"/>
        
        <!-- 行1：预览区 - 保持原有预览功能，图片时使用TagTrain样式 -->
        <Border Grid.Row="1" Background="Transparent" BorderBrush="#E9ECEF" BorderThickness="0,1,0,1">
            <Grid Name="PreviewGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ClipToBounds="True">
                <!-- 原有的预览区域（用于非图片文件） -->
                <TextBlock Text="选择一个文件进行预览" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Foreground="Gray" FontSize="14" Name="DefaultPreviewText"/>
                
                <!-- TagTrain样式的图片预览（叠加层，仅在显示图片时可见） -->
                <Border BorderBrush="#CCCCCC" BorderThickness="2" 
                        Background="#F5F5F5" CornerRadius="5" ClipToBounds="True" 
                        Name="ImagePreviewBorder" Visibility="Collapsed">
                    <Grid ClipToBounds="True">
                        <Image Name="ImagePreviewDisplay" 
                               Stretch="Uniform" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               StretchDirection="Both"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        
                        <!-- 图片导航控件（叠加在图片上方） -->
                        <Grid VerticalAlignment="Bottom" Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 导航按钮 -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" 
                                        HorizontalAlignment="Center" Margin="0,0,0,5">
                                <Button Name="PrevImageBtn" Content="◀ 上一张" 
                                        Padding="10,5" Margin="0,0,5,0" 
                                        Click="PrevImageBtn_Click" 
                                        Visibility="Collapsed"
                                        Background="#E0E0E0" BorderBrush="#CCCCCC"
                                        Cursor="Hand"/>
                                <Button Name="NextImageBtn" Content="下一张 ▶" 
                                        Padding="10,5" 
                                        Click="NextImageBtn_Click" 
                                        Visibility="Collapsed"
                                        Background="#E0E0E0" BorderBrush="#CCCCCC"
                                        Cursor="Hand"/>
                            </StackPanel>
                            
                            <!-- 图片索引显示 -->
                            <TextBlock Name="ImageIndexText" 
                                       Text="第 1/10 张" 
                                       HorizontalAlignment="Center"
                                       FontSize="12" Foreground="#666666"
                                       Cursor="Hand" 
                                       MouseLeftButtonDown="ImageIndexText_MouseLeftButtonDown"
                                       ToolTip="点击跳转到指定图片"
                                       Visibility="Collapsed"
                                       Padding="5,2"/>
                        </Grid>
                        
                        <TextBlock Name="NoImagePreviewText" Text="选择一个文件进行预览" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"
                                  FontSize="18" Foreground="Gray" Visibility="Collapsed"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        
        <!-- 行3：备注区 -->
        <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="0,1,0,0">
            <TextBox Name="NotesTextBox" Padding="8,4,8,4" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     TextChanged="NotesTextBox_TextChanged"
                     Background="#FFFFFF" BorderBrush="Transparent" BorderThickness="0"/>
        </Border>
    </Grid>
</UserControl>
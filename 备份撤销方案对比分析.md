# YiboFile-Core 备份 & 撤销系统 — 方案对比分析

> 2026-05-05 第二轮讨论

---

## 零、一个被忽视的事实

**Windows 文件资源管理器不存在文件操作的 Ctrl+Z。**

你看到的 Explorer Ctrl+Z 只能撤销地址栏文本编辑。文件层面的删除→撤销，完全依赖回收站（Recycle Bin）。

**OneCommander 没有撤销功能。** 开发者 Milos Paripovic 在 Google Groups 回复：

> "Windows doesn't expose the API to access those functions so until I make a custom file operation handler it won't be possible"

这是一个被多次请求但从未实现的功能（2022年1月至今，累计十几条用户请求）。

也就是说，**YiboFile 是目前为数不多的、真正实现了文件操作撤销的 Windows 文件管理器之一**。

---

## 一、三种方案的原理对比

### 方案 A：当前方案（自定义备份目录）

```
删除 → copy文件到 %APPDATA%/YiboFile/Backup/ → 删原文件
撤销 → move回原位置

同卷: File.Move (rename → 快)
跨卷: File.Copy + File.Delete (实际拷贝 → 慢)
目录: Directory.Move / CopyDirectory recursive
```

**本质**：一个自建的迷你"回收站"，维护 manifeset.json 索引。

### 方案 B：Windows 回收站（Recycle Bin）

```
删除 → IFileOperation + FOFX_RECYCLEONDELETE → 系统回收站
撤销 → 从回收站恢复（IContextMenu.InvokeCommand("undelete")）

同卷: 回收站内部是 rename → 极快，毫秒级
跨卷: 回收站内部是 copy → 慢，但这是物理规律无法避免
目录: 回收站原生支持目录
```

**本质**：把存储和管理委托给 Windows 系统。

### 方案 C：硬链接（NTFS Hardlink）

```
删除 → CreateHardLink(原始文件, backupDir/guid.ext) → DeleteFile(原始文件)
撤销 → MoveFileEx(backupDir/guid.ext, 原始路径)

同卷: 无数据拷贝！CreateHardLink 只是 MFT 加一条记录 → <1ms
         MoveFileEx 也只是 rename → <1ms
跨卷: 硬链接不支持，回退到 copy → 慢
目录: NTFS 不支持目录硬链接，回退到 copy → 慢
```

**本质**：把 NTFS 的 inode 共享机制用于"伪备份"。

---

## 二、性能对比（大文件场景）

| 操作 | 方案A (当前) | 方案B (回收站) | 方案C (硬链接) | 最优 |
|------|-------------|----------------|----------------|------|
| 同卷删除 1GB文件 | copy→120MB/s=8秒 再 delete | rename→<1ms | hardlink→<1ms + delete | **B/C** |
| 同卷删除 10GB文件 | copy→80秒 | <1ms | <1ms | **B/C** |
| 跨卷删除 1GB文件 | copy→8秒 + delete | copy→8秒 | 回退copy→8秒 | 一样 |
| 同卷撤销删除 1GB | MoveFileEx→<1ms | Shell恢复→<1ms | MoveFileEx→<1ms | 一样 |
| 跨卷撤销删除 1GB | MoveFileEx→8秒 | Shell恢复→8秒 | MoveFileEx→8秒 | 一样 |
| 同卷删除 含1000文件的目录 | 递归 Move→快 | rename→<1ms | hardlink: 不可用→回退copy | **B** |
| 同卷移动后撤销 | MoveUndo: 再MoveFileEx→<1ms | 不涉及 | 不涉及 | **A** (纯路径记录，无数据拷贝) |

**结论**：
- 当前方案在同卷大文件删除上比回收站/硬链接慢 10000 倍以上（8秒 vs 1毫秒）
- 跨卷场景所有方案性能相同（物理限制）
- 目录删除：只有回收站能零拷贝处理

---

## 三、回收站 API 现状

### 发送到回收站（简单）

```csharp
// .NET 内置方式（推荐）
Microsoft.VisualBasic.FileIO.FileSystem.DeleteFile(
    path,
    UIOption.OnlyErrorDialogs,
    RecycleOption.SendToRecycleBin);

// 或 Win32 IFileOperation（更灵活，支持进度回调）
var fo = new FileOperation() as IFileOperation;
fo.DeleteItem(item);
fo.SetOperationFlags(FOFX_RECYCLEONDELETE | FOF_NOCONFIRMATION);
fo.PerformOperations();
```

### 从回收站恢复（需要 Shell 编程）

**没有直接的 "SHRestoreFileByPath" API。** 必须：

```csharp
// 1. 通过 Shell 命名空间定位回收站文件夹
var shell = new Shell32.Shell();
Folder recycler = shell.NameSpace(0xa); // ssfBITBUCKET = 10

// 2. 遍历查找原始路径匹配的文件
foreach (FolderItem item in recycler.Items())
{
    string originalPath = recycler.GetDetailsOf(item, 1); // column 1 = 原始路径
    if (originalPath == targetPath)
    {
        // 3. 调用 "还原" 动词
        foreach (FolderItemVerb verb in item.Verbs())
        {
            if (verb.Name.Contains("还") || verb.Name.Contains("ESTORE"))
            {
                verb.DoIt();
                break;
            }
        }
    }
}
```

**问题**：
- 回收站内文件名是混淆的（如 `$RXQ2K3P.txt`）
- 必须遍历整个回收站来找匹配项——如果回收站有几千项，会很慢
- 依赖 Shell32 COM 组件，需要 STA 线程

### 更可靠的恢复方式：保存恢复句柄

发送到回收站时，`IFileOperation` 可以获取 `IShellItem` 的 `PIDL`。可以在 `IAsyncOperation` 回调中记录原始路径→回收站路径的映射，恢复时直接用 `IContextMenu.InvokeCommand`。

但这同样复杂。

---

## 四、推荐方案：混合架构

### 核心思想

放弃"把所有操作都先备份再执行"的模式，改为 **"操作直接执行 + 轻量记录 + 回收站兜底"**。

### 新架构

```
                    ┌────────────────────────────┐
                    │      UndoService            │
                    │  (栈管理，不变)              │
                    └──────────┬─────────────────┘
                               │
              ┌────────────────┼────────────────┐
              │                │                │
     ┌────────▼──────┐  ┌──────▼──────┐  ┌─────▼──────┐
     │ MoveUndoAction │  │DeleteUndo   │  │CopyUndo    │
     │ (纯路径记录)    │  │(回收站)      │  │(回收站)    │
     │  src↔dst       │  │             │  │+源路径保留 │
     └───────────────┘  └─────────────┘  └────────────┘
```

### 各操作的处理方式

#### 1. Delete（非永久删除）

```
执行: IFileOperation.DeleteItem → 回收站
记录: DeleteRecord { OriginalPath, DeletionTime }
Undo: 从回收站恢复 → IContextMenu.InvokeCommand("undelete")
Redo: 重新删除 → 回收站

不产生任何自定义备份文件。
```

#### 2. Move（剪切+粘贴）

```
执行: MoveFileEx(src, dst)
记录: MoveRecord { OldPath, NewPath }
Undo: MoveFileEx(dst, src)   ← 同卷 rename (毫秒)
Redo: MoveFileEx(src, dst)

不产生任何备份文件。同卷移动零开销。
```

#### 3. Copy（复制+粘贴）

```
执行: CopyFileEx(src, dst)
记录: CopyRecord { SourcePath, CreatedPath }
Undo: IFileOperation.DeleteItem(dst) → 回收站
Redo: CopyFileEx(src, dst)  ← 需源文件仍存在

也不产生自定义备份。
如果 Redo 时源文件已不存在 → 提示"无法重做"。
```

#### 4. Rename

```
执行: MoveFile(oldName, newName)
记录: RenameRecord { OldPath, NewPath }
Undo: MoveFile(newName, oldName)
Redo: MoveFile(oldName, newName)

零开销。
```

#### 5. New/Create File

```
执行: File.Create(path)
记录: CreateRecord { CreatedPath }
Undo: IFileOperation.DeleteItem(path) → 回收站
Redo: File.Create(path)

零额外开销。
```

### 性能对比

```
操作场景          当前方案耗时    新方案耗时      提升倍数
──────────────────────────────────────────────────────
同卷删1GB文件        ~8秒         <1ms           ~10000x
同卷删10GB文件       ~80秒        <1ms           ~inf
同卷删1000文件目录   可能几秒      <1ms           ~inf
同卷移动后撤销       ~0ms(rename)  ~0ms(rename)   相同
跨卷删1GB文件        ~8秒         ~8秒           相同(物理限制)
跨卷移动后撤销       ~8秒         ~8秒           相同(物理限制)
```

### 完全消除的代码

- `Services/Backup/BackupService.cs` **全部**
- `Services/Backup/BackupRecord.cs` **全部**
- `Services/FileOperations/Undo/BackupRestoreUndoAction.cs` **全部**
- `Services/FileOperations/Undo/BackupCleanupService.cs` **全部**
- `Services/FileSystem/FileSystemCoreUtils.cs` 中的 `SafeDeleteFileAsync`、`SafeDeleteDirectoryAsync`、`CopyDirectory`
- `Models/Config/UserSettings.cs` 中的 `BackupSettings`
- `Controls/Settings/BackupSettingsPanel.xaml.cs`
- Manifest.json 的并发问题 **自动消失**
- 两个备份目录不统一的问题 **自动消失**

### 保留的代码

- `UndoService.cs` — 栈管理（几乎不变）
- `UndoableActions.cs` — 精简的 UndoableAction 子类（路径记录版）
- `FileOperationService.cs` — 入口逻辑调整

---

## 五、回收站方案的已知限制及对策

| 限制 | 影响 | 对策 |
|------|------|------|
| 回收站恢复需遍历Shell | 回收站有上千项时，恢复变慢（可能数秒） | 记录删除时间+文件名做索引，先精确匹配再遍历 |
| 回收站满 → 自动清理 | 旧文件可能无法恢复 | 操作前检查回收站容量 `SHQueryRecycleBin`，给用户提示 |
| Shell COM 需要 STA 线程 | WPF 主线程天然 STA，问题不大 | 恢复操作放在 Dispatcher.BeginInvoke 中 |
| 回收站不支持映射网络驱动器 | 删网络文件时退化为永久删除 | 删除前检测路径类型，网络路径提醒用户 |
| 用户可能手动清空回收站 | Redo 可能失败 | 这是合理的——用户主动清空意味着不需要了 |
| 改名冲突 | 同路径恢复时原位置已有同名文件 | IFileOperation 自带冲突对话框 |

---

## 六、迁移路径建议

分三个阶段：

### 第一阶段：Delete 接入回收站（最大收益）

- `FileOperationService.DeleteAsync` 中 permanent=false 的分支改为 `FileSystem.DeleteFile(path, RecycleOption.SendToRecycleBin)`
- 实现一个简单的 `RecycleBinService` 封装恢复逻辑
- 新建 `DeleteUndoActionV2` 使用回收站记录
- 保留旧的 BackupService 不删，只断开调用

### 第二阶段：清理备份系统

- 移除 BackupService、BackupCleanupService、BackupRestoreUndoAction
- 简化 MoveUndoAction、CopyUndoAction 为纯路径记录
- 移除设置面板的备份相关 UI

### 第三阶段：优化边界情况

- 回收站容量预检
- 网络路径回退
- 大目录删除的进度显示

---

## 七、最终建议

**采用混合架构，核心原则：**

1. **Delete → 回收站**：性能从"拷贝整个文件"变为"重命名一条MFT记录"，同卷操作提升 10000 倍
2. **Move → 路径记录**：不产生任何备份文件，同卷撤销也是 rename 级别
3. **Copy → 回收站兜底**：撤销就是删到回收站，可恢复
4. **废弃所有自定义备份目录**：不需要 manifest.json、不需要 BackupCleanupService、不需要 BackupSettings 配置——全部依赖 Windows 回收站的内置管理

**代码量估算**：减少约 1500 行（BackupService 246行 + BackupCleanupService 127行 + BackupRestoreUndoAction 80行 + UndoableActions 中旧逻辑 ~500行 + 各种配置/UI ~300行），新增约 200 行（RecycleBinService），净减少 ~1300 行。

前提是需要先验证 IFileOperation 回收站恢复机制的可靠性，建议做个 Spike 验证 P/Invoke 调用链。

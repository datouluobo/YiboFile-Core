<Window x:Class="OoiMRR.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OoiMRR"
        xmlns:controls="clr-namespace:OoiMRR.Controls"
        Title="OoiMRR - 文件资源管理器" 
        Height="800" 
        Width="1200"
        MinHeight="600"
        MinWidth="960"
        WindowState="Normal"
        WindowStyle="SingleBorderWindow"
        AllowsTransparency="False"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        Background="{DynamicResource AppBackgroundBrush}"
        PreviewKeyDown="MainWindow_PreviewKeyDown"
        Closing="Window_Closing"
        LocationChanged="Window_LocationChanged">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="50" 
                      ResizeBorderThickness="4" 
                      GlassFrameThickness="1" 
                      CornerRadius="0" 
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <local:TimeUnitConverter x:Key="TimeUnitConverter"/>
    </Window.Resources>
    
    <!-- 主容器：包含标题栏和窗口内容 -->
    <Grid Name="MainContainer">
        <!-- 窗口内容容器 -->
    <Grid Name="RootGrid" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Margin" Value="4"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                        <Setter Property="Margin" Value="8"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <!-- 左中右3列布局 -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColLeft" Width="220" MinWidth="220"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition x:Name="ColCenter" Width="*" MinWidth="250"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition x:Name="ColRight" Width="360" MinWidth="360"/>
        </Grid.ColumnDefinitions>
        
        <!-- 布局：标题栏行 + 标签页行 + 内容行 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="51"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏背景（包含底部边框） -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5"
                Background="Transparent"
                BorderBrush="{DynamicResource BorderSubtleBrush}"
                BorderThickness="0,0,0,1"
                Panel.ZIndex="1000"/>
        
        <!-- 导航按钮：位于列0 (随列0宽度自动调整) -->
        <Grid Name="NavButtonsContainer" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0" Panel.ZIndex="1001">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 导航按钮区域：固定宽度，左对齐 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="8,0" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Name="NavPathBtn" Click="NavPathBtn_Click" Margin="2,0" Height="32" Width="60"
                        Style="{StaticResource ActiveNavigationButtonStyle}" Tag="Selected"> <!-- 默认选中路径 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{DynamicResource Icon_Nav_Path}" FontFamily="{DynamicResource IconFontFamily}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="路径" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Name="NavLibraryBtn" Click="NavLibraryBtn_Click" Margin="2,0" Height="32" Width="60"
                        Style="{StaticResource ActiveNavigationButtonStyle}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{DynamicResource Icon_Nav_Library}" FontFamily="{DynamicResource IconFontFamily}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="库" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <!-- NavTagBtn Phase 2将重新引入
                <Button Name="NavTagBtn" Click="NavTagBtn_Click" Margin="2,0" Height="32" Width="60"
                        Style="{StaticResource ActiveNavigationButtonStyle}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{DynamicResource Icon_Nav_Tag}" FontFamily="{DynamicResource IconFontFamily}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="标签" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                -->
            </StackPanel>
            
            <!-- 分隔符 (隐藏保留) -->
            <Separator Name="TitleBarSeparator" Visibility="Collapsed" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Height="24" Width="1"/>
        </Grid>

        <!-- 标签页管理器：从列1(Splitter)开始，跨越剩余列 -->
        <controls:TabManagerControl Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4"
                                    x:Name="TabManager" 
                                    Margin="0,0,380,0"
                                    VerticalAlignment="Center"
                                    Panel.ZIndex="1001"/>



        <!-- ========== 标签页管理器 ========== -->


        <!-- 垂直分割器（从Row 2开始，跨越2行：Row 2和Row 3） -->
        <!-- 左侧导航与中间文件列表之间的分割器 -->
        <controls:CollapsibleGridSplitter x:Name="SplitterLeft" Grid.Row="2" Grid.Column="1" Grid.RowSpan="2"
                      Style="{StaticResource VerticalCollapsibleGridSplitterStyle}"
                      CollapseMode="Previous"
                      ShowsPreview="True"
                      Panel.ZIndex="2000"
                      DragCompleted="GridSplitter_DragCompleted"/>
        
        <!-- 中间文件列表与右侧预览之间的分割器 -->
        <controls:CollapsibleGridSplitter x:Name="SplitterRight" Grid.Row="2" Grid.Column="3" Grid.RowSpan="2"
                      Style="{StaticResource VerticalCollapsibleGridSplitterStyle}"
                      CollapseMode="Both"
                      ShowsPreview="True"
                      Panel.ZIndex="2000"
                      AutoFillNeighbor="True"
                      DragCompleted="GridSplitter_DragCompleted"/>

        <!-- ========== 列1：导航区（独立控件） ========== -->
        <controls:NavigationPanelControl Grid.Row="2" Grid.Column="0" Grid.RowSpan="2" x:Name="NavigationPanelControl"/>

        <!-- ========== 列2：文件显示区 ========== -->
        <controls:FileBrowserControl x:Name="FileBrowser"
                                     Grid.Column="2" 
                                     Grid.Row="1" 
                                     Grid.RowSpan="3"
                                     HorizontalAlignment="Stretch" 
                                     VerticalAlignment="Stretch"
                                     Margin="0,0,0,0"/>

        <!-- ========== 列3：预览/备注区 ========== -->
        <local:RightPanelControl Grid.Row="0" Grid.Column="4" Grid.RowSpan="4"
                                 x:Name="RightPanel"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"/>

        <!-- 设置覆盖层:覆盖所有内容列,不包括标题栏 -->
        <Grid Name="SettingsOverlay"
              Grid.Row="1" Grid.RowSpan="3"
              Grid.Column="0" Grid.ColumnSpan="5"
              Background="{DynamicResource AppBackgroundBrush}"
              Panel.ZIndex="5000"
              Visibility="Collapsed">
            <controls:SettingsPanelControl x:Name="SettingsPanel"/>
        </Grid>

        <!-- 关于覆盖层 -->
        <Grid Name="AboutOverlay"
              Grid.Row="1" Grid.RowSpan="3"
              Grid.Column="0" Grid.ColumnSpan="5"
              Background="{DynamicResource AppBackgroundBrush}"
              Panel.ZIndex="5000"
              Visibility="Collapsed">
            <controls:AboutPanelControl x:Name="AboutPanel"/>
        </Grid>
            <!-- 右上角窗口控制按钮：移动到 RootGrid 内部以保持 Margin 一致性 -->


        <!-- 通知容器: 位于底部中央，覆盖在最上层 -->
        <StackPanel x:Name="NotificationContainer"
                    Grid.Row="1" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="5"
                    VerticalAlignment="Bottom" HorizontalAlignment="Center"
                    Margin="0,0,0,20"
                    Panel.ZIndex="9000"/>

    </Grid>
    
    <!-- 右上角窗口控制按钮：移动到 MainContainer 直接子元素，脱离 RootGrid 布局限制 -->
    <Grid Name="WindowControlButtonsContainer" 
          HorizontalAlignment="Right" VerticalAlignment="Top"
          Height="51"
          Panel.ZIndex="10000"
          PreviewMouseDown="WindowControlButtonsContainer_PreviewMouseDown">
        
        <!-- 按钮区域 -->
        <StackPanel x:Name="WindowButtonsStackPanel" 
                   Orientation="Horizontal"
                   HorizontalAlignment="Right" VerticalAlignment="Stretch"
                   Background="{DynamicResource AppBackgroundBrush}"
                   WindowChrome.IsHitTestVisibleInChrome="True">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Margin" Value="0,0,0,0"/>
                    <Style.Triggers>
                        <!-- 最大化时添加右边距，避免贴合屏幕边缘 -->
                        <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                            <Setter Property="Margin" Value="0,0,8,0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            
            <!-- 布局模式切换按钮组 -->
            <Border Width="1" Height="24" 
                    Background="{DynamicResource BorderSubtleBrush}"
                    Margin="4,0" VerticalAlignment="Center"/>
            
                <!-- 专注模式：全屏中间 -->
                <Button x:Name="LayoutFocusBtn" Width="46"
                        Content="{DynamicResource Icon_Layout_Focus}"
                        ToolTip="专注模式 (Ctrl+Shift+F)&#x0a;折叠左右，最大化文件列表"
                        Click="LayoutFocus_Click"
                        Background="Transparent" BorderThickness="0" Cursor="Hand"
                        FontSize="16" FontFamily="{DynamicResource IconFontFamily}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Active">
                                    <Setter Property="Background" Value="{DynamicResource AccentDefaultBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundOnAccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- 工作模式：左+中间 -->
                <Button x:Name="LayoutWorkBtn" Width="46"
                        Content="{DynamicResource Icon_Layout_Work}"
                        ToolTip="工作模式 (Ctrl+Shift+W)&#x0a;左导航+文件列表"
                        Click="LayoutWork_Click"
                        Background="Transparent" BorderThickness="0" Cursor="Hand"
                        FontSize="16" FontFamily="{DynamicResource IconFontFamily}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Active">
                                    <Setter Property="Background" Value="{DynamicResource AccentDefaultBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundOnAccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- 完整模式：全部显示 -->
                <Button x:Name="LayoutFullBtn" Width="46"
                        Content="{DynamicResource Icon_Layout_Full}"
                        ToolTip="完整模式 (Ctrl+Shift+A)&#x0a;三栏完整布局"
                        Click="LayoutFull_Click"
                        Background="Transparent" BorderThickness="0" Cursor="Hand"
                        FontSize="16" FontFamily="{DynamicResource IconFontFamily}"
                        Tag="Active">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Active">
                                    <Setter Property="Background" Value="{DynamicResource AccentDefaultBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundOnAccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            
            <!-- 分隔线 -->
            <Border Width="1" Height="24" 
                    Background="{DynamicResource BorderSubtleBrush}"
                    Margin="4,0" VerticalAlignment="Center"/>
            
            <!-- 设置按钮 -->
            <Button x:Name="TitleBarSettingsButton" Content="{DynamicResource Icon_Window_Settings}" 
                    Width="46"
                    Click="Settings_Click" 
                    ToolTip="设置" FontSize="{DynamicResource WindowControlIconSize}" FontFamily="{DynamicResource IconFontFamily}"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <!-- 关于按钮 -->
            <Button x:Name="TitleBarAboutButton" Content="{DynamicResource Icon_Window_About}" 
                    Width="46"
                    Click="About_Click" 
                    ToolTip="关于" FontSize="{DynamicResource WindowControlIconSize}" FontFamily="{DynamicResource IconFontFamily}"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <!-- 最小化按钮 -->
            <Button x:Name="TitleBarMinimizeButton" Content="{DynamicResource Icon_Window_Minimize}" 
                    Width="46"
                    Click="WindowMinimize_Click" 
                    ToolTip="最小化" FontSize="{DynamicResource WindowControlIconSize}" FontFamily="{DynamicResource IconFontFamily}"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="TitleBarMaxRestoreButton" Content="{DynamicResource Icon_Window_Maximize}" 
                    Width="46"
                    Click="WindowMaximize_Click" 
                    ToolTip="最大化" FontSize="{DynamicResource WindowControlIconSize}" FontFamily="{DynamicResource IconFontFamily}"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="TitleBarCloseButton" Content="{DynamicResource Icon_Window_Close}" 
                    Width="46"
                    Click="WindowClose_Click" 
                    ToolTip="关闭" FontSize="{DynamicResource WindowControlIconSize}" FontFamily="{DynamicResource IconFontFamily}"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource CloseButtonHoverBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource CloseButtonPressedBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
        

    </Grid>
</Window>

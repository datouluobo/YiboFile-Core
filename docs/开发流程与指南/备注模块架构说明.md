# 备注模块 (Notes Module) 现代化架构设计

## 概述

备注模块是本项目首个采用完全现代化架构重构的模块，用于验证和展示最佳实践。
该架构可作为后续模块（如标签、收藏夹、库管理）重构的参考模板。

## 架构层次

```
┌─────────────────────────────────────────────────────────────┐
│                     UI 层 (View)                             │
│  FileNotesUIHandler.cs, RightPanelControl                   │
└─────────────────────────────────────────────────────────────┘
                              ↓ 消息总线
┌─────────────────────────────────────────────────────────────┐
│                  ViewModel 层 (Module)                       │
│  NotesModule.cs                                              │
│  - 订阅消息: GetNotes, SaveNotes, DeleteNotes, Search       │
│  - 发布消息: NotesLoaded, NotesUpdated                      │
└─────────────────────────────────────────────────────────────┘
                              ↓ 依赖注入
┌─────────────────────────────────────────────────────────────┐
│                   服务层 (Service)                           │
│  INotesService / NotesService                                │
│  - 业务逻辑封装                                              │
│  - 事件触发 (NotesUpdated)                                   │
│  - 摘要生成、批量操作等                                      │
└─────────────────────────────────────────────────────────────┘
                              ↓ 接口抽象
┌─────────────────────────────────────────────────────────────┐
│                  数据访问层 (Repository)                     │
│  INotesRepository / SqliteNotesRepository                    │
│  - CRUD 操作                                                 │
│  - FTS5 全文搜索                                             │
│  - 批量查询优化                                              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据库 (SQLite)                           │
│  FileNotes 表 + FileNotesFts 虚拟表                         │
└─────────────────────────────────────────────────────────────┘
```

## 文件结构

```
Services/
├── Data/
│   └── Repositories/
│       ├── INotesRepository.cs        # 接口定义
│       └── SqliteNotesRepository.cs   # SQLite 实现
└── Features/
    └── FileNotes/
        ├── NotesService.cs            # 业务服务层
        ├── INotesService.cs           # (在 NotesService.cs 内)
        └── FileNotesUIHandler.cs      # UI 事件处理

ViewModels/
├── Modules/
│   └── NotesModule.cs                 # MVVM 模块
└── Messaging/
    └── Messages/
        └── NotesMessages.cs           # 消息定义
```

## 核心设计原则

### 1. 接口抽象 (INotesRepository)
- 定义清晰的数据访问契约
- 便于单元测试（可 Mock）
- 支持多种实现（SQLite、文件、云存储等）

### 2. 依赖注入 (DI)
在 `App.xaml.cs` 中注册：
```csharp
services.AddSingleton<INotesRepository, SqliteNotesRepository>();
services.AddSingleton<INotesService, NotesService>();
```

### 3. 消息驱动 (Message Bus)
UI 与 ViewModel 通过消息解耦：
- `GetNotesRequestMessage` → 请求加载备注
- `SaveNotesRequestMessage` → 请求保存备注
- `NotesLoadedMessage` → 备注加载完成通知
- `NotesUpdatedMessage` → 备注更新通知

### 4. 向后兼容
`FileNotesUIHandler` 支持两种模式：
- **新架构**: 通过 `INotesService` 操作
- **旧架构**: 回退至 `FileNotesService` 静态类

## 使用方式

### 方式一：通过消息总线
```csharp
// 保存备注
_messageBus.Publish(new SaveNotesRequestMessage(filePath, notes));

// 订阅加载完成
_messageBus.Subscribe<NotesLoadedMessage>(msg => {
    textBox.Text = msg.Notes;
});
```

### 方式二：通过 NotesModule 直接调用
```csharp
var notesModule = _viewModel.Notes;
await notesModule.SaveNotesAsync(filePath, notes);
var summary = notesModule.GetSummary(notes);
```

### 方式三：通过 DI 获取服务
```csharp
var notesService = App.ServiceProvider.GetService<INotesService>();
var notes = await notesService.GetNotesAsync(filePath);
```

## 性能优化

### 批量获取
```csharp
// 一次查询多个文件的备注
var notesBatch = await notesModule.GetNotesBatchAsync(filePaths);
```

### FTS5 全文搜索
- 中文逐字前缀匹配
- 英文单词前缀匹配
- 自动 LIKE 回退

## 后续模块迁移计划

| 模块 | 接口 | 实现 | 状态 |
|------|------|------|------|
| 备注 | INotesRepository | SqliteNotesRepository | ✅ 完成 |
| 标签 | ITagsRepository | SqliteTagsRepository | 📋 待开发 |
| 收藏夹 | IFavoritesRepository | SqliteFavoritesRepository | 📋 待开发 |
| 库管理 | ILibraryRepository | SqliteLibraryRepository | 📋 待开发 |

## 测试建议

由于采用了接口抽象，可以轻松编写单元测试：

```csharp
[TestClass]
public class NotesServiceTests
{
    [TestMethod]
    public async Task SaveAndLoad_ShouldReturnSameContent()
    {
        var mockRepo = new Mock<INotesRepository>();
        mockRepo.Setup(r => r.GetNotes("test.txt")).Returns("Hello");
        
        var service = new NotesService(mockRepo.Object);
        var notes = service.GetNotes("test.txt");
        
        Assert.AreEqual("Hello", notes);
    }
}
```

---
*文档版本: 1.0*
*最后更新: 2026-01-25*

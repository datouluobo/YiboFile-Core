<Window x:Class="OoiMRR.TagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新建标签" Height="200" Width="300"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="标签名称:"/>
        <TextBox Grid.Row="1" Name="TagNameTextBox" Margin="0,5"/>
        
        <Label Grid.Row="2" Content="标签颜色:" Margin="0,10,0,0"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <Button Name="ColorButton" Content="选择颜色" Click="ColorButton_Click" Margin="0,0,10,0"/>
            <Rectangle Name="ColorPreview" Width="30" Height="20" Fill="Red" Stroke="Black"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Click="OK_Click" Margin="0,0,10,0" Width="60"/>
            <Button Content="取消" Click="Cancel_Click" Width="60"/>
        </StackPanel>
    </Grid>
</Window>

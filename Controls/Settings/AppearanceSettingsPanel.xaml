<UserControl x:Class="YiboFile.Controls.Settings.AppearanceSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i18n="clr-namespace:YiboFile.Services.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,0,16,0">
        <StackPanel Margin="0,0,0,24">
            
            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.Language}" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding AvailableLanguages}" DisplayMemberPath="NativeName" SelectedValuePath="Code" SelectedValue="{Binding SelectedLanguage, Mode=TwoWay}" />

            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.Theme}" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />
            
            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.ThemeDesc}" FontSize="12" Margin="0,0,0,16" TextWrapping="Wrap" Foreground="{DynamicResource ForegroundSecondaryBrush}" />
            
            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.SelectTheme}" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding Themes}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedTheme, Mode=TwoWay}" />
            
            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.IconStyle}" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding IconStyles}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedIconStyle, Mode=TwoWay}" />
            
            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.UIStyle}" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding UIStyles}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedUIStyle, Mode=TwoWay}" />

            <TextBlock Text="{i18n:Loc Key=Settings.Appearance.GlobalFontSize}" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBox MaxLength="3" Text="{Binding UIFontSizeInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Width="60" Height="32" VerticalContentAlignment="Center" TextAlignment="Center" Padding="5,0,5,0" Margin="0,0,8,0" Grid.Column="0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <EventSetter Event="KeyDown" Handler="NumericTextBox_KeyDown" />
                            <EventSetter Event="LostFocus" Handler="NumericTextBox_LostFocus" />
                            <EventSetter Event="PreviewTextInput" Handler="NumericTextBox_PreviewTextInput" />
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <StackPanel Orientation="Vertical" Grid.Column="1" VerticalAlignment="Center">
                    <Button Content="▲" Width="24" Height="16" FontSize="10" Padding="0" Margin="0,0,0,2" VerticalAlignment="Center" Click="UIFontSizeUp_Click" Style="{DynamicResource ModernButtonStyle}" />
                    <Button Content="▼" Width="24" Height="16" FontSize="10" Padding="0" VerticalAlignment="Center" Click="UIFontSizeDown_Click" Style="{DynamicResource ModernButtonStyle}" />
                </StackPanel>
                <TextBlock Text="{i18n:Loc Key=Settings.Appearance.GlobalFontSizeDesc}" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center" Margin="16,0,0,0" Grid.Column="2" TextWrapping="Wrap" />
            </Grid>

        </StackPanel>
    </Grid>
</UserControl>

<UserControl x:Class="YiboFile.Controls.Settings.AppearanceSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,0,16,0">
        <StackPanel Margin="0,0,0,24">
            
            <TextBlock Text="主题设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />
            
            <TextBlock Text="选择应用程序的颜色主题。跟随系统将根据Windows主题自动切换。" FontSize="12" Margin="0,0,0,16" TextWrapping="Wrap" Foreground="{DynamicResource ForegroundSecondaryBrush}" />
            
            <TextBlock Text="选择主题：" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding Themes}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedTheme, Mode=TwoWay}" />
            
            <TextBlock Text="图标风格：" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding IconStyles}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedIconStyle, Mode=TwoWay}" />
            
            <TextBlock Text="UI 方案 (界面风格)：" FontSize="14" FontWeight="Medium" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            
            <ComboBox MinHeight="40" Padding="12,0,12,0" VerticalContentAlignment="Center" FontSize="14" HorizontalAlignment="Stretch" Margin="0,0,0,24"
                      ItemsSource="{Binding UIStyles}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedUIStyle, Mode=TwoWay}" />

        </StackPanel>
    </Grid>
</UserControl>

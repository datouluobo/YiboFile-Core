<UserControl x:Class="YiboFile.Controls.Settings.SearchSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Focusable="True" FocusVisualStyle="{x:Null}"
             d:DesignHeight="800" d:DesignWidth="800">
    <StackPanel x:Name="MainStackPanel" Margin="20">
        <StackPanel.Resources>
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <EventSetter Event="KeyDown" Handler="NumericTextBox_KeyDown" />
                <EventSetter Event="LostFocus" Handler="NumericTextBox_LostFocus" />
                <EventSetter Event="PreviewTextInput" Handler="NumericTextBox_PreviewTextInput" />
            </Style>
        </StackPanel.Resources>
        
        <TextBlock Text="全文搜索 (Full Text Search)" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
        <TextBlock Text="启用全文搜索后，您可以使用 'content:关键词' 或切换到全文模式搜索文档内容。" FontSize="12" Margin="0,0,0,24" TextWrapping="Wrap" Foreground="{DynamicResource ForegroundSecondaryBrush}" />

        <GroupBox Header="Everything 搜索引擎 (文件名)" Margin="0,0,0,24" Padding="10" Background="Transparent" BorderThickness="0">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="当前版本:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                    <TextBlock x:Name="EverythingVersionText" TextWrapping="Wrap" VerticalAlignment="Center" Grid.Column="1" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                </Grid>
                <Button Content="强制重建 Everything 索引" Click="RebuildEverythingButton_Click" Padding="16,8,16,8" HorizontalAlignment="Left" Margin="0,10,0,10" Cursor="Hand" Style="{DynamicResource ModernButtonStyle}" />
                <TextBlock Text="Everything 通常会自动维护索引。仅在搜索结果不准确或遗漏文件时使用此功能。" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource ForegroundSecondaryBrush}" />
            </StackPanel>
        </GroupBox>

        <CheckBox Content="启用全文索引与搜索" IsChecked="{Binding IsEnableFullTextSearch}" FontSize="14" MinHeight="32" Margin="0,0,0,24" Foreground="{DynamicResource ForegroundPrimaryBrush}" />

        <GroupBox Header="全文索引状态" Margin="0,0,0,24" Padding="10" Background="Transparent" BorderThickness="0">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="索引数据库位置:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                    <TextBlock Text="{Binding IndexLocation}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,10,0" Grid.Column="1" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                    <Button Content="更改" Click="ChangeIndexLocationButton_Click" Padding="8,2,8,2" Grid.Column="2" Style="{DynamicResource ModernButtonStyle}" />
                </Grid>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="已索引文档数:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                    <TextBlock Text="{Binding IndexedFileCount}" TextWrapping="Wrap" VerticalAlignment="Center" Grid.Column="1" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                </Grid>
                <StackPanel Margin="0,5,0,0">
                    <ProgressBar Value="{Binding IndexingProgress}" Height="6" Margin="0,5,0,5" Maximum="100" />
                    <TextBlock Text="{Binding IndexingStatusText}" FontSize="12" Opacity="0.7" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="搜索历史与地址栏" Margin="0,0,0,24" Padding="10" Background="Transparent" BorderThickness="0">
            <StackPanel>
                <CheckBox Content="地址栏获得焦点时自动展开历史记录" IsChecked="{Binding AutoExpandHistory}" FontSize="14" Margin="0,0,0,10" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="保留历史记录数量:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                    <TextBox MaxLength="6" Text="{Binding HistoryMaxCountInput, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Padding="4" Grid.Column="1" />
                </Grid>
                <Button Content="清除所有历史记录" Click="ClearHistoryButton_Click" Padding="12,6,12,6" HorizontalAlignment="Left" Style="{DynamicResource ModernButtonStyle}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="维护操作" Margin="0,0,0,24" Padding="10" Background="Transparent" BorderThickness="0">
            <StackPanel>
                <TextBlock Text="索引范围 (为空时默认扫描所有库)" FontWeight="Bold" Margin="0,0,0,5" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                <ListBox x:Name="ScopeListBox" ItemsSource="{Binding IndexScopes}" Height="100" Margin="0,0,0,5" SelectionMode="Extended" Background="{DynamicResource BackgroundPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <Button Content="添加目录" Click="AddScopeButton_Click" Padding="10,4,10,4" Margin="0,0,10,0" Style="{DynamicResource ModernButtonStyle}" />
                    <Button Content="移除选中" Click="RemoveScopeButton_Click" Padding="10,4,10,4" Style="{DynamicResource ModernButtonStyle}" />
                </StackPanel>
                <Button Content="重建索引" Click="RebuildIndexButton_Click" Padding="16,8,16,8" HorizontalAlignment="Left" Margin="0,0,0,10" Style="{DynamicResource ModernButtonStyle}" />
                <TextBlock Text="重建索引会清空当前所有索引数据并重新扫描。对于大型文档库可能需要较长时间。后台任务将以低优先级运行。" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource ForegroundSecondaryBrush}" />
            </StackPanel>
        </GroupBox>

        <Expander Header="支持的文件格式" Margin="0,0,0,24" Foreground="{DynamicResource ForegroundPrimaryBrush}">
            <StackPanel x:Name="ExtensionsPanel" Margin="10" />
        </Expander>

    </StackPanel>
</UserControl>

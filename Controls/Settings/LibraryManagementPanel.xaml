<UserControl x:Class="YiboFile.Controls.Settings.LibraryManagementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:YiboFile.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800"
             Background="White">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:MathConverter x:Key="MathConverter" />
        
        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                         <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
             <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
             <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="库管理" FontWeight="SemiBold" FontSize="16" Margin="15,15,15,5"/>

        <!-- Main Content: List of Libraries (Expanders) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10">
            <ItemsControl x:Name="LibrariesList" BorderThickness="0" Background="Transparent" AlternationCount="2">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" BorderThickness="1" BorderBrush="#EEEEEE"
                                Background="#FAFAFA" CornerRadius="4">
                            <Expander IsExpanded="True" BorderThickness="0" Background="Transparent" Padding="5">
                            <Expander.Header>
                                <Grid Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Expander}}, Path=ActualWidth, Converter={StaticResource MathConverter}, ConverterParameter=-50}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Icon -->
                                    <TextBlock Text="📚" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    
                                    <!-- Library Name -->
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="14"/>

                                    <!-- Library Actions -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="🔼" Click="MoveLibraryUp_Click" Style="{StaticResource ModernButtonStyle}" 
                                                Width="24" Height="24" Padding="0" Margin="4,0,0,0" ToolTip="上移"
                                                Background="Transparent" BorderThickness="0"/>
                                        <Button Content="🔽" Click="MoveLibraryDown_Click" Style="{StaticResource ModernButtonStyle}" 
                                                Width="24" Height="24" Padding="0" Margin="4,0,0,0" ToolTip="下移"
                                                Background="Transparent" BorderThickness="0"/>
                                        <Button Content="✎" Click="RenameLibrary_Click" Style="{StaticResource ModernButtonStyle}" 
                                                Width="24" Height="24" Padding="0" Margin="4,0,0,0" ToolTip="重命名库"
                                                Background="Transparent" BorderThickness="0"/>
                                        <Button Content="❌" Click="DeleteLibrary_Click" Style="{StaticResource ModernButtonStyle}" 
                                                Width="24" Height="24" Padding="0" Margin="4,0,0,0" ToolTip="删除库" Foreground="#FF5555"
                                                Background="Transparent" BorderThickness="0"/>
                                    </StackPanel>
                                </Grid>
                            </Expander.Header>
                            
                            <Expander.Content>
                                <Grid Margin="0,5,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Paths List -->
                                    <ListBox ItemsSource="{Binding Paths}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="Margin" Value="0"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <ContentPresenter/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#F0F0F0" CornerRadius="4" Padding="8,6" Margin="4,2">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding Path}" Foreground="Gray" FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                        </StackPanel>
                                                        
                                                        <StackPanel x:Name="ActionButtons" Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0"
                                                                    Opacity="0">
                                                             <Button Content="✎" Click="EditPath_Click" Width="20" Height="20" Padding="0"
                                                                     Background="Transparent" BorderThickness="0" ToolTip="编辑显示名称" Margin="0,0,2,0"/>
                                                             <Button Content="✕" Click="RemovePath_Click" Width="20" Height="20" Padding="0" 
                                                                     Background="Transparent" BorderThickness="0" ToolTip="移除位置" Foreground="#FF5555"/>
                                                        </StackPanel>
                                                    </Grid>
                                                    <Border.Triggers>
                                                        <EventTrigger RoutedEvent="MouseEnter">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetName="ActionButtons" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                        <EventTrigger RoutedEvent="MouseLeave">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetName="ActionButtons" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                    </Border.Triggers>
                                                </Border>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>

                                    <!-- Add Path Inline Button -->
                                    <Button Grid.Row="1" Content="+ 添加位置" Margin="4,5,4,4" Click="AddPath_Click" 
                                            Style="{StaticResource ModernButtonStyle}" Background="#EFEFEF" Height="30"
                                            HorizontalContentAlignment="Left" Padding="10,0"/>
                                </Grid>
                            </Expander.Content>
                        </Expander>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer: Add Library and Import/Export -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#EEEEEE" Background="#FAFAFA" Padding="15">
            <StackPanel>
                <!-- Add Library -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewLibraryNameTextBox" Tag="新建库..." Style="{StaticResource ModernTextBoxStyle}" KeyDown="NewLibraryNameTextBox_KeyDown"/>
                    <TextBlock Text="新建库..." Foreground="#808080" Margin="8,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=NewLibraryNameTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <Button Grid.Column="1" Content="新建库" Padding="15,0" Margin="10,0,0,0" Click="NewLibrary_Click" Style="{StaticResource ModernAccentButtonStyle}" Height="32"/>
                </Grid>
                
                <!-- Import/Export -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="导入配置" Click="ImportBtn_Click" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"/>
                    <Button Content="导出配置" Click="ExportBtn_Click" Style="{StaticResource ModernButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Error Snackbar -->
        <Border x:Name="ErrorOverlay" Grid.RowSpan="3" VerticalAlignment="Bottom" Background="#B00020" Padding="15,10" Margin="20" CornerRadius="4" Visibility="Collapsed">
            <TextBlock x:Name="ErrorText" Text="Error Message" Foreground="White" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>

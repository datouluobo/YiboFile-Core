<UserControl x:Class="YiboFile.Controls.Settings.LibrarySettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i18n="clr-namespace:YiboFile.Services.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Border Padding="20" Background="{DynamicResource BackgroundSecondaryBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{i18n:Loc Key=Settings.Library.Title}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource ForegroundPrimaryBrush}" Grid.Row="0" />
            <TextBlock Text="{i18n:Loc Key=Settings.Library.Desc}" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,15" Foreground="{DynamicResource ForegroundSecondaryBrush}" Grid.Row="1" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15" Grid.Row="2">
                <Button Content="{i18n:Loc Key=Settings.Library.AddLibrary}" Command="{Binding AddLibraryCommand}" Padding="15,8,15,8" Background="{DynamicResource AccentDefaultBrush}" Foreground="{DynamicResource ForegroundOnAccentBrush}" BorderThickness="0" Cursor="Hand" FontSize="13" Style="{DynamicResource ModernAccentButtonStyle}" />
            </StackPanel>

            <ItemsControl ItemsSource="{Binding Libraries}" Grid.Row="3">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,10" Background="{DynamicResource BackgroundTertiaryBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="{DynamicResource ControlDefaultBrush}" Margin="0,0,15,0" VerticalAlignment="Center">
                                    <TextBlock Text="📚" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" Foreground="{DynamicResource ForegroundPrimaryBrush}"/>
                                    <TextBlock Text="{Binding DisplayPath}" FontSize="12" Foreground="{DynamicResource ForegroundSecondaryBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ToolTipText}"/>
                                </StackPanel>

                                <Button Grid.Column="2" Content="✕" 
                                        Command="{Binding DataContext.RemoveLibraryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"
                                        Width="32" Height="32" 
                                        Background="Transparent" BorderThickness="0" Foreground="#EF5350" Cursor="Hand" ToolTip="{i18n:Loc Key=Settings.Library.RemoveLibrary}"
                                        Style="{DynamicResource ModernButtonStyle}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>

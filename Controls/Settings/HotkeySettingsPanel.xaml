<UserControl x:Class="YiboFile.Controls.Settings.HotkeySettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i18n="clr-namespace:YiboFile.Services.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20" Grid.Row="0">
            <TextBlock Text="{i18n:Loc Key=Settings.Hotkeys.ManagementTitle}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource ForegroundPrimaryBrush}" />
            <TextBlock Text="{i18n:Loc Key=Settings.Hotkeys.ManagementDesc}" FontSize="14" Margin="0,8,0,0" Foreground="{DynamicResource ForegroundSecondaryBrush}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,15" Grid.Row="1">
            <Button Content="{i18n:Loc Key=Settings.Hotkeys.RestoreDefault}" Padding="16,8,16,8" MinWidth="100" Cursor="Hand" Command="{Binding ResetHotkeysCommand}" Style="{DynamicResource ModernButtonStyle}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Hotkeys}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="15,6,15,6" Padding="12" CornerRadius="8" Background="{DynamicResource BackgroundSecondaryBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderLightBrush}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="SkyBlue" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="{Binding Description}" VerticalAlignment="Center" FontSize="14" Foreground="{DynamicResource ForegroundPrimaryBrush}" Grid.Column="0" />
                                
                                <Border Background="{DynamicResource AccentLightBrush}" BorderBrush="{DynamicResource AccentDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="8,2,8,2" Margin="10,0,10,0" VerticalAlignment="Center" SnapsToDevicePixels="True" Grid.Column="1">
                                    <TextBlock Text="{Binding KeyCombination}" FontFamily="Consolas, Segoe UI" FontWeight="SemiBold" Foreground="{DynamicResource AccentDefaultBrush}" />
                                </Border>
                                
                                <StackPanel Orientation="Horizontal" Grid.Column="2">
                                    <Button Content="{i18n:Loc Key=Settings.Hotkeys.Edit}" Margin="5,0,0,0" Padding="12,4,12,4" Click="EditHotkey_Click" Tag="{Binding .}" Style="{DynamicResource ModernButtonStyle}" />
                                    <!-- Use dynamic resource and bindings normally -->
                                    <Button Content="{i18n:Loc Key=Settings.Hotkeys.Restore}" Margin="8,0,0,0" Padding="12,4,12,4" Click="ResetHotkey_Click" Tag="{Binding .}" Style="{DynamicResource ModernButtonStyle}"
                                            Visibility="{Binding IsModified, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border Width="1" Background="LightGray" VerticalAlignment="Stretch" HorizontalAlignment="Center" Margin="0,10,0,10" SnapsToDevicePixels="True" IsHitTestVisible="False" />
        </Grid>

        <TextBlock Text="{i18n:Loc Key=Settings.Hotkeys.EditHint}" FontSize="12" Margin="0,15,0,0" Foreground="{DynamicResource ForegroundSecondaryBrush}" Grid.Row="3" />
    </Grid>
</UserControl>

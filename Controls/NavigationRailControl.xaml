<UserControl x:Class="YiboFile.Controls.NavigationRailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YiboFile.Controls"
             xmlns:helpers="clr-namespace:YiboFile.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="60"
             Background="{DynamicResource SidebarBackgroundBrush}"
             BorderBrush="{DynamicResource DividerBrush}"
             BorderThickness="0,0,1,0">

    <UserControl.Resources>
        <ResourceDictionary>
            <helpers:StringEqualsConverter x:Key="StringEqualsConverter"/>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            
            <!-- Rail Button Style -->
            <Style x:Key="RailButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="Transparent" 
                                BorderThickness="0"
                                CornerRadius="{DynamicResource UI.Button.CornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentLightBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ResourceDictionary>
</UserControl.Resources>

    <Grid AllowDrop="True" Drop="Spacer_Drop" Tag="BottomSpacer">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Top: Navigation -->
            <RowDefinition Height="*"/>    <!-- Middle: Tools (Spacer) -->
            <RowDefinition Height="Auto"/> <!-- Bottom: Global -->
        </Grid.RowDefinitions>

        <!-- Top Section: Navigation Modes -->
        <ItemsControl Grid.Row="0" Margin="0,12,0,0" ItemsSource="{Binding TopItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding Command}"
                            Style="{StaticResource RailButtonStyle}"
                            ToolTip="{Binding ToolTip}"
                            IsEnabled="{Binding IsEnabled}"
                            Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                            AllowDrop="True"
                            PreviewMouseLeftButtonDown="RailItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="RailItem_PreviewMouseMove"
                            Drop="RailItem_Drop"
                            DragOver="RailItem_DragOver">
                        <Image helpers:IconHelper.IconKey="{Binding IconKey}" Width="18" Height="18" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Spacer region for dropping to Top vs Bottom if we drop in the middle -->
        <Border Grid.Row="1" Background="Transparent" AllowDrop="True" Drop="Spacer_Drop" Tag="TopSpacer"/>

        <!-- Bottom Section: Global Settings & Layout -->
        <ItemsControl Grid.Row="2" Margin="0,0,0,12" ItemsSource="{Binding BottomItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding Command}"
                            Style="{StaticResource RailButtonStyle}"
                            ToolTip="{Binding ToolTip}"
                            IsEnabled="{Binding IsEnabled}"
                            Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                            AllowDrop="True"
                            PreviewMouseLeftButtonDown="RailItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="RailItem_PreviewMouseMove"
                            Drop="RailItem_Drop"
                            DragOver="RailItem_DragOver">
                        <Image helpers:IconHelper.IconKey="{Binding IconKey}" Width="18" Height="18" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>



<UserControl x:Class="YiboFile.Controls.NavTaskQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YiboFile.Controls"
             xmlns:helpers="clr-namespace:YiboFile.Helpers"
             xmlns:tq="clr-namespace:YiboFile.Services.FileOperations.TaskQueue"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="250"
             Visibility="Visible"
             Background="{DynamicResource NavigationRegionBrush}">
    <UserControl.Resources>
        <local:NavTaskStatusToColorConverter x:Key="StatusToColor"/>
        <local:NavStatusToVisibilityConverter x:Key="StatusToVis"/>
        
        <!-- Compact Progress Bar for Task Queue -->
        <Style x:Key="CompactProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="4"/>
            <Setter Property="Background" Value="{DynamicResource ControlDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2" ClipToBounds="True">
                            <Grid>
                                <Border x:Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        CornerRadius="2"
                                        HorizontalAlignment="Left">
                                    <Border.Width>
                                        <MultiBinding Converter="{x:Static local:NavProgressWidthConverter.Instance}">
                                            <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}"/>
                                            <Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                            <Binding Path="ActualWidth" RelativeSource="{RelativeSource TemplatedParent}"/>
                                        </MultiBinding>
                                    </Border.Width>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Icon Button Style -->
        <Style x:Key="IconButtonButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="{DynamicResource UI.Button.CornerRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                 <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="Transparent" Padding="8,6" MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown" Cursor="Hand" ToolTip="折叠/展开后台任务">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="⚡" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                
                <TextBlock Grid.Column="1" Text="{Binding TaskCountText}" FontWeight="SemiBold" FontSize="13"
                           VerticalAlignment="Center" Foreground="{DynamicResource ForegroundPrimaryBrush}"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Click="ClearCompleted_Click" 
                            Style="{StaticResource IconButtonButtonStyle}" 
                            Width="22" Height="22" Margin="0,0,2,0"
                            ToolTip="清理已完成任务">
                        <Image Source="{DynamicResource Icon_Delete_Outline}" Width="12" Height="12" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>
                    <ToggleButton x:Name="ExpandToggle"
                                  Width="22" Height="22"
                                  IsChecked="True"
                                  Checked="ExpandToggle_Checked"
                                  Unchecked="ExpandToggle_Unchecked">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="{DynamicResource UI.Button.CornerRadius}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ToggleButton.Style>
                        <Path x:Name="ExpandArrow" Data="M 1,0 L 4.5,4 L 8,0" Stroke="{DynamicResource ForegroundSecondaryBrush}" StrokeThickness="1.5"
                              HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" RenderTransformOrigin="0.5,0.5">
                              <Path.RenderTransform>
                                  <RotateTransform Angle="180"/>
                              </Path.RenderTransform>
                        </Path>
                    </ToggleButton>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Tasks List -->
        <ScrollViewer x:Name="ListScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="8,0,8,8">
            <ItemsControl x:Name="TasksList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type tq:FileOperationTask}">
                        <Grid Margin="0,4,0,8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <DockPanel Grid.Row="0" Margin="0,0,0,4" LastChildFill="True">
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0,0,0">
                                    <Ellipse Width="6" Height="6" Margin="0,0,4,0"
                                             Fill="{Binding Status, Converter={StaticResource StatusToColor}}"/>
                                    <Button Tag="{Binding}" Click="PauseResume_Click" 
                                            Style="{StaticResource IconButtonButtonStyle}" Width="20" Height="20"
                                            Visibility="{Binding Status, Converter={StaticResource StatusToVis}, ConverterParameter=CanPause}">
                                        <Image Source="{DynamicResource Icon_Pause}" Width="10" Height="10"/>
                                    </Button>
                                    <Button Tag="{Binding}" Click="CancelTask_Click" 
                                            Style="{StaticResource IconButtonButtonStyle}" Width="20" Height="20" Margin="2,0,0,0"
                                            Visibility="{Binding Status, Converter={StaticResource StatusToVis}, ConverterParameter=CanCancel}">
                                        <Image helpers:IconHelper.IconKey="Icon_Close" Width="10" Height="10"/>
                                    </Button>
                                </StackPanel>

                                <TextBlock Text="{Binding Description}" 
                                           FontWeight="Medium" FontSize="12"
                                           Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                            </DockPanel>
                            
                            <ProgressBar Grid.Row="1" 
                                         Value="{Binding Progress}" Maximum="100" 
                                         Style="{StaticResource CompactProgressBarStyle}"
                                         Foreground="{Binding Status, Converter={StaticResource StatusToColor}}"
                                         Margin="0,2,0,4"/>
                            
                            <TextBlock Grid.Row="2" Text="{Binding CurrentFile}" 
                                       FontSize="11" Opacity="0.6"
                                       Foreground="{DynamicResource ForegroundSecondaryBrush}" 
                                       TextTrimming="CharacterEllipsis" 
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

<UserControl x:Class="YiboFile.Controls.InnerPreviewAttachmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:YiboFile.Controls"
             xmlns:viewModels="clr-namespace:YiboFile.ViewModels"
             xmlns:local="clr-namespace:YiboFile"
             xmlns:converters="clr-namespace:YiboFile.Converters"
             xmlns:previews="clr-namespace:YiboFile.ViewModels.Previews"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="100">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:DoubleToGridLengthConverter x:Key="DoubleToGridLengthConverter"/>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            
            <Style x:Key="VerticalTabRadioButtonStyle" TargetType="RadioButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource FocusSecondaryBrush}"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentDefaultBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundOnAccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0" Background="Transparent">
        <Grid.ColumnDefinitions>
            <!-- 内容列 -->
            <ColumnDefinition Width="*"/>
            <!-- 窄按钮列 -->
            <ColumnDefinition Width="28"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧的内容区域 -->
        <Border Grid.Column="0" Background="Transparent" BorderThickness="0" Margin="0,4,4,4">
            <Grid>
                <!-- 预览内容 (当BtnPreview选中时显示) -->
                <Grid x:Name="PreviewArea" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ClipToBounds="True" Background="Transparent">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=BtnPreview}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.Resources>
                        <ResourceDictionary>
                            <ResourceDictionary.MergedDictionaries>
                                <ResourceDictionary Source="pack://application:,,,/YiboFile-Core;component/Styles/Controls/PreviewTemplates.xaml"/>
                            </ResourceDictionary.MergedDictionaries>

                            <!-- 为小预览自定义极简 DataTemplate，优先级高于全局模板 -->
                            
                            <!-- 图片极简预览 -->
                            <DataTemplate DataType="{x:Type previews:ImagePreviewViewModel}">
                                <Image Source="{Binding ImageSource}" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant"/>
                            </DataTemplate>

                            <!-- PDF 极简预览 (隐藏工具栏) -->
                            <DataTemplate DataType="{x:Type previews:PdfPreviewViewModel}">
                                <controls:PdfView FilePath="{Binding FilePath}" HideToolbar="True"/>
                            </DataTemplate>

                            <!-- Markdown 极简预览 -->
                            <DataTemplate DataType="{x:Type previews:MarkdownPreviewViewModel}">
                                <controls:MarkdownPreviewControl/>
                            </DataTemplate>

                            <!-- 文本极简预览 (只读，自动换行) -->
                            <DataTemplate DataType="{x:Type previews:TextPreviewViewModel}">
                                <TextBox Text="{Binding Content, Mode=OneWay}" 
                                         AcceptsReturn="True" 
                                         IsReadOnly="True" 
                                         TextWrapping="Wrap"
                                          FontFamily="Consolas" 
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Auto"
                                          Background="Transparent"
                                          Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                          BorderThickness="0"
                                          Padding="5"/>
                            </DataTemplate>

                            <!-- 媒体极简预览 (视频/音频) -->
                            <DataTemplate DataType="{x:Type previews:MediaPreviewViewModel}">
                                <Grid>
                                    <controls:VideoPreviewControl FilePath="{Binding FilePath}" IsPlaying="{Binding IsPlaying, Mode=TwoWay}" Visibility="{Binding IsVideo, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <controls:AudioPreviewControl FilePath="{Binding FilePath}" Visibility="{Binding IsAudio, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- 视频播放按钮覆盖层 (仅在视频且未播放时显示) -->
                                    <Button HorizontalAlignment="Center" VerticalAlignment="Center" 
                                            Width="50" Height="50"
                                            Command="{Binding PlayPauseCommand}"
                                            Background="#80000000"
                                            BorderThickness="0"
                                            Content="▶"
                                            Foreground="White"
                                            FontSize="24"
                                            Padding="5,0,0,0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid Background="Transparent">
                                                    <Ellipse Fill="{TemplateBinding Background}" Width="50" Height="50"/>
                                                    <TextBlock Text="▶" Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3,0,0,0"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsVideo}" Value="True"/>
                                                            <Condition Binding="{Binding IsPlaying}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </DataTemplate>

                            <!-- 文档极简预览 (Word) -->
                            <DataTemplate DataType="{x:Type previews:WordPreviewViewModel}">
                                <Grid>
                                    <controls:WordPreviewControl/>
                                    <Border Background="{DynamicResource OverlayDarkBrush}" Visibility="{Binding NeedsConversion, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding ConversionMessage}" Foreground="{DynamicResource ForegroundPrimaryBrush}" TextWrapping="Wrap" Margin="10" TextAlignment="Center"/>
                                            <Button Content="立即转换" Command="{Binding ConvertCommand}" Style="{DynamicResource PrimaryButtonStyle}" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </DataTemplate>

                            <!-- 表格极简预览 (Excel) -->
                            <DataTemplate DataType="{x:Type previews:ExcelPreviewViewModel}">
                                <controls:ExcelPreviewControl/>
                            </DataTemplate>

                            <!-- 文档极简预览 (PPT) -->
                            <DataTemplate DataType="{x:Type previews:PowerPointPreviewViewModel}">
                                <controls:PowerPointPreviewControl/>
                            </DataTemplate>

                            <!-- 压缩包极简预览 -->
                            <DataTemplate DataType="{x:Type previews:ArchivePreviewViewModel}">
                                <ListView ItemsSource="{Binding Entries}" Background="Transparent" Foreground="{DynamicResource ForegroundPrimaryBrush}" BorderThickness="0">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                            <GridViewColumn Header="大小" DisplayMemberBinding="{Binding Size}" Width="80"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </DataTemplate>

                            <!-- 文件夹内容极简预览 -->
                            <DataTemplate DataType="{x:Type previews:FolderPreviewViewModel}">
                                <ListView ItemsSource="{Binding Items}" Background="Transparent" Foreground="{DynamicResource ForegroundPrimaryBrush}" BorderThickness="0">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                            <GridViewColumn Header="大小" DisplayMemberBinding="{Binding Size}" Width="80"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </DataTemplate>
                        </ResourceDictionary>
                    </Grid.Resources>

                    <ContentPresenter Content="{Binding ActivePreview}"/>

                    <!-- 加载中遮罩 -->
                    <Grid Visibility="{Binding ActivePreview.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" Background="{DynamicResource OverlayLightBrush}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="100" Height="4" Margin="0,0,0,10"/>
                            <TextBlock Text="加载中..." Foreground="{DynamicResource ForegroundPrimaryBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 没有任何项被选中时 -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ActivePreview}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="无可用预览" Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="13"/>
                    </StackPanel>
                </Grid>

                <!-- 笔记内容 (当BtnNotes选中时显示) -->
                <Grid x:Name="NotesArea" Background="Transparent">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=BtnNotes}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <TextBox Name="NotesTextBox" Padding="8" 
                             AcceptsReturn="True" 
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             Text="{Binding CurrentNotes, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent" 
                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                             BorderBrush="Transparent" BorderThickness="0"/>
                </Grid>
            </Grid>
        </Border>

        <!-- 右侧的竖排选项卡按钮（等分高度） -->
        <Grid Grid.Column="1" Margin="0,4,0,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <RadioButton x:Name="BtnPreview" Grid.Row="0" Margin="0,0,0,2" IsChecked="True" Style="{DynamicResource VerticalTabRadioButtonStyle}" MinHeight="70" ToolTip="预览文件" FocusVisualStyle="{x:Null}">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="预" FontSize="13" Margin="0,0,0,4" HorizontalAlignment="Center"/>
                    <TextBlock Text="览" FontSize="13" HorizontalAlignment="Center"/>
                </StackPanel>
            </RadioButton>
            <RadioButton x:Name="BtnNotes" Grid.Row="1" Margin="0,2,0,0" Style="{DynamicResource VerticalTabRadioButtonStyle}" MinHeight="70" ToolTip="文件笔记" FocusVisualStyle="{x:Null}">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="笔" FontSize="13" Margin="0,0,0,4" HorizontalAlignment="Center"/>
                    <TextBlock Text="记" FontSize="13" HorizontalAlignment="Center"/>
                </StackPanel>
            </RadioButton>
        </Grid>
    </Grid>

</UserControl>

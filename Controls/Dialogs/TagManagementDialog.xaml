<Window x:Class="YiboFile.Controls.Dialogs.TagManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YiboFile.Controls.Dialogs"
        mc:Ignorable="d"
        Title="标签管理" Height="500" Width="800"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="White"
        Foreground="#333333">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Simply Style for ListBoxItem to stretch content -->
        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4" Margin="0,1">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Groups -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="分组列表" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>

            <ListBox x:Name="GroupsList" Grid.Row="1" SelectionChanged="GroupsList_SelectionChanged" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Display Mode -->
                            <TextBlock x:Name="DisplayName" Text="{Binding Name}" VerticalAlignment="Center"/>
                            
                            <!-- Inline Edit Mode (Initially Collapsed) -->
                            <TextBox x:Name="EditNameBox" Text="{Binding Name}" Visibility="Collapsed" 
                                     KeyDown="EditNameBox_KeyDown" LostFocus="EditNameBox_LostFocus"/>

                            <!-- Action Buttons (Hover Only) -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" 
                                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button Content="✎" Click="RenameGroup_Click" Style="{StaticResource ModernButtonStyle}" 
                                        Width="24" Height="24" Padding="0" Margin="4,0,0,0" ToolTip="重命名"
                                        Background="Transparent" BorderThickness="0"/>
                                <Button Content="❌" Click="DeleteGroup_Click" Style="{StaticResource ModernButtonStyle}" 
                                        Width="24" Height="24" Padding="0" Margin="4,0,0,0" ToolTip="删除" Foreground="#FF5555"
                                        Background="Transparent" BorderThickness="0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Add Group Footer -->
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewGroupNameBox" Tag="新建分组..." Style="{StaticResource ModernTextBoxStyle}" KeyDown="NewGroupNameBox_KeyDown"/>
                <Button Grid.Column="1" Content="+" Width="30" Margin="5,0,0,0" Click="AddGroupBtn_Click" Style="{StaticResource ModernAccentButtonStyle}"/>
            </Grid>
        </Grid>

        <!-- Separator -->
        <Border Grid.Column="1" Background="{DynamicResource BorderBrush}" Margin="0,10"/>

        <!-- Right Panel: Tags -->
        <Grid Grid.Column="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="标签列表" FontWeight="SemiBold" FontSize="16"/>
                <TextBlock x:Name="CurrentGroupNameText" Text="" Foreground="{DynamicResource TextSecondaryBrush}" Margin="10,2,0,0"/>
            </StackPanel>

            <ListBox x:Name="TagsList" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding Color}" CornerRadius="12" Padding="8,4" Margin="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" Foreground="White" VerticalAlignment="Center"/>
                                <StackPanel x:Name="ActionButtons" Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0"
                                            Opacity="0">
                                     <Button Content="✎" Click="RenameTag_Click" Width="16" Height="16" Padding="0"
                                             Background="Transparent" BorderThickness="0" Foreground="White" ToolTip="重命名" Margin="0,0,2,0"/>
                                     <Button Content="✕" Click="DeleteTag_Click" Width="16" Height="16" Padding="0" 
                                             Background="Transparent" BorderThickness="0" Foreground="White" ToolTip="删除"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}}" Value="True">
                                <Setter TargetName="ActionButtons" Property="Opacity" Value="1"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

             <!-- Empty State Hint -->
            <TextBlock x:Name="NoGroupSelectedHint" Grid.Row="1" Text="请先在左侧选择一个分组" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"
                       Visibility="Visible"/>

            <!-- Add Tag Footer -->
            <Grid Grid.Row="2" Margin="0,10,0,0" x:Name="AddTagArea" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Color Picker (Simple Dropdown or random?) -> Let's keep it simple for now, maybe just text -->
                <TextBox Grid.Column="1" x:Name="NewTagNameBox" Tag="新建标签..." Style="{StaticResource ModernTextBoxStyle}" KeyDown="NewTagNameBox_KeyDown"/>
                <Button Grid.Column="2" Content="添加" Padding="10,0" Margin="5,0,0,0" Click="AddTagBtn_Click" Style="{StaticResource ModernAccentButtonStyle}"/>
            </Grid>
        </Grid>
        
        <!-- Error Snackbar (Simple overlay) -->
        <Border x:Name="ErrorOverlay" Grid.ColumnSpan="3" VerticalAlignment="Bottom" Background="#B00020" Padding="15,10" Margin="20" CornerRadius="4" Visibility="Collapsed">
            <TextBlock x:Name="ErrorText" Text="Error Message" Foreground="White" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>

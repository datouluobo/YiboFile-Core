<UserControl x:Class="YiboFile.Controls.PaneContentHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:YiboFile.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid x:Name="RootGrid">
        <!-- 文件浏览器 (默认显示, 始终保持实例化) -->
        <controls:FileBrowserControl x:Name="FileBrowserView"/>

        <!-- 自定义面板容器 (特殊标签页时显示) -->
        <Grid x:Name="CustomContentContainer" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 地址栏 -->
                <RowDefinition Height="*"/>     <!-- 内容区 -->
            </Grid.RowDefinitions>

            <!-- Row 0: 面板地址栏 (复用 AddressBarControl, 视觉完全一致) -->
            <Border Grid.Row="0" Background="Transparent" Margin="0,4,0,4">
                <controls:AddressBarControl x:Name="ContentAddressBar" Margin="8,0"
                                           IsReadOnly="False"/>
            </Border>

            <!-- Row 1: 面板内容 -->
            <ContentPresenter x:Name="CustomContentPresenter" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>

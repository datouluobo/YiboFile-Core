<UserControl x:Class="OoiMRR.Controls.TabManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Border x:Name="TabsBorder" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="0" Visibility="Visible">
        <Grid Margin="8,4">
            <!-- 标签页区域 -->
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel x:Name="TabsPanel" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>


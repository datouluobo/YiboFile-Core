<UserControl x:Class="OoiMRR.Controls.SettingsPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:OoiMRR.Controls"
             MinWidth="800" MinHeight="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- 左侧导航栏 -->
        <Border Grid.Column="0" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 标题栏 -->
                <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E9ECEF" BorderThickness="0,0,0,1" Padding="12,12">
                    <TextBlock Text="设置" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                </Border>
                
                <!-- 搜索框 -->
                <Border Grid.Row="1" Background="#FFFFFF" Padding="10" BorderBrush="#E9ECEF" BorderThickness="0,0,0,1">
                    <TextBox Name="SearchTextBox" 
                             TextChanged="SearchTextBox_TextChanged"
                             FontSize="14" Padding="10,6"
                             VerticalContentAlignment="Center"
                             MinHeight="36"/>
                </Border>
                
                <!-- 设置分类列表 -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ListBox Name="CategoryListBox"
                             BorderThickness="0"
                             Background="Transparent"
                             SelectionChanged="CategoryListBox_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="12,14"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="MinHeight" Value="44"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#333333"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                        <Setter Property="Foreground" Value="#1976D2"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBoxItem Tag="General">通用</ListBoxItem>
                        <ListBoxItem Tag="FileList">文件列表</ListBoxItem>
                        <ListBoxItem Tag="Path">路径</ListBoxItem>
                        <ListBoxItem Tag="Library">库</ListBoxItem>
                        <ListBoxItem Tag="Tag">标签</ListBoxItem>
                        <ListBoxItem Tag="TagTrain">TagTrain</ListBoxItem>
                        <ListBoxItem Tag="Hotkeys">快捷键</ListBoxItem>
                    </ListBox>
                </ScrollViewer>
            </Grid>
        </Border>
        
        <!-- 分割线 -->
        <Border Grid.Column="1" Background="#E9ECEF"/>
        
        <!-- 右侧内容区 -->
        <Border Grid.Column="2" Background="#FFFFFF">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel Name="ContentPanel">
                    <!-- 设置内容将动态加载到这里 -->
                </StackPanel>
            </ScrollViewer>
        </Border>
        
        <!-- 右上角关闭按钮：独立的Grid确保可交互，不拦截其他事件 -->
        <Grid Grid.Column="0" Grid.ColumnSpan="3"
              HorizontalAlignment="Right" VerticalAlignment="Top"
              Background="Transparent"
              Width="50" Height="36"
              Panel.ZIndex="100">
            <Button Name="CloseButton" Content="✕" 
                    Width="40" Height="36"
                    Background="Transparent" BorderThickness="0"
                    Cursor="Hand" Click="CloseButton_Click"
                    ToolTip="关闭设置"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Foreground" Value="#666666"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E5E5E5"/>
                                <Setter Property="Foreground" Value="#000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</UserControl>


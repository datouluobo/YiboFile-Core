<UserControl x:Class="YiboFile.Controls.TagBrowsePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Management actions -->
        <Border Grid.Row="0" Margin="0,0,0,8" Padding="4,4,10,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="标签" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                <Button Grid.Column="1" x:Name="ManageTagsBtn" Content="⚙" ToolTip="管理标签" 
                        Width="30" Height="30" Click="ManageTagsBtn_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        Background="Transparent" Margin="0,0,-5,0"/>
            </Grid>
        </Border>

        <!-- Tags List - Simplified version without complex styles -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TagGroupsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander IsExpanded="True" BorderThickness="0" Background="Transparent">
                            <Expander.Header>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4"/>
                            </Expander.Header>
                            <ItemsControl ItemsSource="{Binding Tags}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="Transparent" Padding="8,4" Margin="0,1"
                                                Cursor="Hand" CornerRadius="4"
                                                MouseLeftButtonDown="TagItem_MouseLeftButtonDown">
                                            <StackPanel Orientation="Horizontal">
                                                <Border Width="10" Height="10" CornerRadius="5" Margin="0,0,8,0" VerticalAlignment="Center"
                                                        Background="{Binding Color, TargetNullValue=#0078D4}"/>
                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Empty State -->
        <TextBlock x:Name="EmptyStateText" Grid.Row="1" 
                   Text="暂无标签" 
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                   Foreground="{DynamicResource TextDisabledBrush}"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>

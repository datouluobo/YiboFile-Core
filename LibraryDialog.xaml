<Window x:Class="YiboFile.LibraryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加库" Height="200" Width="400"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="库名称:"/>
        <TextBox Grid.Row="1" Name="LibraryNameTextBox" Margin="0,5"/>
        
        <Label Grid.Row="2" Content="库路径:" Margin="0,10,0,0"/>
        
        <Grid Grid.Row="3" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" Name="LibraryPathTextBox" Margin="0,0,5,0"/>
            <Button Grid.Column="1" Content="浏览..." Click="BrowseButton_Click"/>
        </Grid>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Click="OK_Click" Margin="0,0,10,0" Width="60"/>
            <Button Content="取消" Click="Cancel_Click" Width="60"/>
        </StackPanel>
    </Grid>
</Window>


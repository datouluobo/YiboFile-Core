# YiboFile-Core 备份系统分析报告

> 生成时间：2026-05-05
> 分析范围：`F:\Download\GitHub\YiboFile\YiboFile-Core`

---

## 一、架构总览

整个系统包含 **三个备份子系统**，分别承担不同角色：

```
┌──────────────────────────────────────────────────────────┐
│              YiboFile-Core 备份系统                         │
│                                                          │
│  ┌─────────────────────┐  ┌───────────────────────────┐  │
│  │  BackupService       │  │  DeleteUndoAction         │  │
│  │  (用户可见备份)       │  │  (撤销栈独立备份)          │  │
│  │                      │  │                           │  │
│  │  %LOCALAPPDATA%      │  │  %LOCALAPPDATA%           │  │
│  │  /YiboFile/Backup/   │  │  /YiboFile/UndoBackup/    │  │
│  │  └─ yyyyMMdd/        │  │  └─ GUID/                │  │
│  │     └─ manifest.json │  │                           │  │
│  │     └─ {guid}.ext    │  │                           │  │
│  └──────────┬───────────┘  └───────────┬───────────────┘  │
│             │                          │                   │
│  ┌──────────▼──────────────────────────▼───────────────┐  │
│  │  BackupRestoreUndoAction                             │  │
│  │  (桥梁：将 BackupService 包装为撤销操作)              │  │
│  └──────────────────────┬──────────────────────────────┘  │
│                         │                                  │
│  ┌──────────────────────▼──────────────────────────────┐  │
│  │  UndoService                                        │  │
│  │  (撤销栈管理，50项上限，Redo/Undo 双向切换)          │  │
│  └──────────────────────┬──────────────────────────────┘  │
│                         │                                  │
│  ┌──────────────────────▼──────────────────────────────┐  │
│  │  BackupCleanupService                                │  │
│  │  (仅清理 UndoBackup 目录，500MB/7天/100项)          │  │
│  └─────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

### 各子系统职责

| 组件 | 文件 | 职责 |
|------|------|------|
| `IBackupService` / `BackupService` | `Services/Backup/` | 用户可见的备份中心：按日期组织、维护 manifest.json、支持恢复/删除/清理 |
| `BackupRestoreUndoAction` | `Undo/BackupRestoreUndoAction.cs` | 桥接层：将 BackupService 包装成 Undo/Redo 可切换操作 |
| `DeleteUndoAction` | `Undo/UndoableActions.cs` | 旧式备份：文件移至 UndoBackup 目录 (BackupService 不可用时的降级方案) |
| `NewFileUndoAction` | `Undo/UndoableActions.cs` | 新建/复制撤销：撤销时移至 UndoBackup，Redo 时恢复 |
| `MoveUndoAction` | `Undo/UndoableActions.cs` | 移动撤销：记录路径对，反向 Move（不走备份系统） |
| `UndoService` | `Undo/UndoService.cs` | 撤销栈管理：50 项上限，发布 `UndoStackChangedMessage` |
| `BackupCleanupService` | `Undo/BackupCleanupService.cs` | 仅清理 UndoBackup 目录；在 WindowLifecycleHandler 中调用 |
| `BackupViewModel` | `ViewModels/BackupViewModel.cs` | 备份管理页面的 ViewModel |
| `BackupSettingsPanel` | `Controls/Settings/BackupSettingsPanel.xaml.cs` | 备份设置面板 |
| `BackupSettings` | `Models/Config/UserSettings.cs` | `BackupDirectory` + `RetentionDays` 配置项 |

---

## 二、数据流

### 2.1 删除操作（非永久）

```
用户删除
  → FileOperationService.DeleteAsync (L394-492)
    → BackupService.CreateBackupAsync(sourcePath)
      └─ 同卷: Directory.Move / File.Move → 移至 /Backup/yyyyMMdd/{guid}.ext
      └─ 跨卷: Copy + SafeDeleteFileAsync → 复制+删除原文件
      └─ 更新 manifest.json
    → BackupRestoreUndoAction(record) 添加到撤销栈
    → CompositeUndoAction → UndoService.RecordAction()
```

### 2.2 粘贴（复制模式）撤销

```
用户粘贴(复制)
  → FileOperationService.PasteAsync (L100-388)
    → BackupRestoreUndoAction(backupService, destPath)  // 文件在磁盘
      └─ Undo时: CreateBackupAsync → 文件入备份库
      └─ Redo时: RestoreAsync → 文件回磁盘
```

### 2.3 剪切（移动模式）撤销 — 不走 BackupService

```
用户剪切粘贴 (isCut=true)
  → FileOperationService.PasteAsync
    → MoveUndoAction(sourcePath, destPath, isDir)
      └─ Undo时: SafeMove(dest, src)  // 反向移动
      └─ Redo时: SafeMove(src, dest)  // 正向重做
```

### 2.4 清理触发点

```
WindowLifecycleHandler (L63):
  → BackupCleanupService.Cleanup()      // 程序退出时清理 UndoBackup 目录

UndoService.Clear() (L149-155):
  → DeleteUndoAction.CleanupBackup()    // 撤销栈清空时清理
  → NewFileUndoAction.CleanupBackup()

BackupViewModel.LoadBackupsAsync() (L97-99):
  → BackupService.CleanOldBackupsAsync() // 加载备份列表时按 retention 清理
```

---

## 三、发现的问题

### 3.1 🔴 两个独立的备份目录，无统一管理

**严重程度**: 中

**描述**:
- `BackupService` 写 `%LOCALAPPDATA%/YiboFile/Backup/`，有 manifest.json 和按日期组织
- `DeleteUndoAction/NewFileUndoAction` 写 `%LOCALAPPDATA%/YiboFile/UndoBackup/`，无 manifest，纯 GUID 目录名
- `BackupCleanupService` 只清理 UndoBackup 目录
- 用户界面的"备份管理"（BackupViewModel）只展示 BackupService 的内容
- UndoBackup 目录中的残留文件无法通过 UI 查看或管理

**涉及文件**: `BackupService.cs`, `UndoableActions.cs (DeleteUndoAction)`, `BackupCleanupService.cs`

### 3.2 🔴 BackupSettings.BackupDirectory 配置未生效

**严重程度**: 高

**描述**:
`BackupService.cs` L21-23 硬编码了备份路径：
```csharp
_backupRoot = Path.Combine(
    Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
    "YiboFile", "Backup");
```
而 `BackupSettings.BackupDirectory` 虽然能从设置面板配置（用户通过 FolderBrowserDialog 选择），但 BackupService 构造函数从未读取这个配置。

**影响**: 用户在设置面板配置的备份目录被完全忽略。

### 3.3 🔴 Manifest 并发竞争风险

**严重程度**: 高

**描述**:
`UpdateManifestAsync` (BackupService.cs L218-244) 的操作序列为：
```
1. ReadAllTextAsync → 读取 JSON
2. JsonSerializer.Deserialize → 反序列化
3. specificUpdate(manifest) → 修改内存对象
4. JsonSerializer.Serialize → 序列化
5. WriteAllTextAsync → 写回文件
```
没有文件锁。如果两个线程同时为同一日期目录操作，后写的 manifest 会覆盖前一个的变更，**导致备份记录丢失**。

**场景**: 同时选中多个大文件删除，每个文件各自触发 CreateBackupAsync。

### 3.4 🟡 备份文件用 GUID 命名，丢失原始文件名信息

**严重程度**: 低

**描述**:
```csharp
// BackupService.cs L53-54
var backupFileName = $"{record.Id}{ext}";
```
文件只保留扩展名不保留原名。用户直接通过资源管理器浏览备份目录时（例如 `%LOCALAPPDATA%/YiboFile/Backup/20260505/`），看到的是 `a1b2c3d4e5f6.txt` 这样的文件，无法识别原始文件。

### 3.5 🟡 设置面板"清理"按钮未实现

**严重程度**: 中

**描述**:
```csharp
// BackupSettingsPanel.cs L118-119
CleanButton_Click:
  MessageBox.Show("清理功能将在下一阶段实现。", "提示");
```
UI 上有"清理备份"按钮，但只弹出提示消息框，没有实际执行清理。

### 3.6 🟡 两个保留策略不一致且不联动

**严重程度**: 中

**描述**:

| 策略点 | 默认值 | 适用范围 | 可否配置 |
|--------|--------|----------|----------|
| `BackupCleanupService.MaxBackupAgeDays` | 7 天 | UndoBackup 目录 | 否（静态属性） |
| `BackupCleanupService.MaxBackupSizeMB` | 500 MB | UndoBackup 目录 | 否（静态属性） |
| `BackupCleanupService.MaxBackupCount` | 100 | UndoBackup 目录 | 否（静态属性） |
| `BackupSettings.RetentionDays` | 30 天 | BackupService 目录 | 是（设置面板） |

- UndoBackup 的清理参数不可配置，且和 BackupService 的 30 天默认值不一致
- 用户改 `BackupRetentionDays` 为 60，UndoBackup 仍是 7 天自动清理

### 3.7 🟢 跨卷备份的大文件耗时无进度反馈

**严重程度**: 低

**描述**:
`BackupService.CreateBackupAsync` 中跨卷操作为 Copy+Delete，在 `Task.Run` 内执行但没有进度回调。当删除大文件（如数 GB 的视频）且 Backup 目录在另一磁盘时，用户会看到"正在删除..."但没有进度条更新。

对比：`FileOperationService.PasteAsync` 中的跨卷移动使用了 `NativeFileOperations.MoveFile` 带进度回调，体验更优。

### 3.8 🟢 BackupService 没有总大小限制

**严重程度**: 低

**描述**:
- `BackupCleanupService` 有 `MaxBackupSizeMB = 500`，但只对 UndoBackup 目录生效
- `BackupService.CleanOldBackupsAsync` 只按天数清理，没有总大小硬限制
- 如果用户每天删大量文件（如编译产出），BackupService 目录可能膨胀到数 GB

### 3.9 🟢 UndoService.Clear() 不清理 BackupService 的备份

**严重程度**: 低

**描述**:
```csharp
// UndoService.cs L149-155
if (action is DeleteUndoAction deleteAction)
    deleteAction.CleanupBackup();
```
只清理了 `DeleteUndoAction` 的 UndoBackup 文件。`BackupRestoreUndoAction` 包裹的 BackupService 记录在栈清空时不会被删除，这些备份会永远留在 `/YiboFile/Backup/` 中，直到 retention 超期。

### 3.10 🟢 BackupRestoreUndoAction 的路径漂泊问题

**严重程度**: 低

**描述**:
```csharp
// BackupService.cs L122-125 (RestoreAsync)
if (File.Exists(destPath) || Directory.Exists(destPath))
    destPath = FileSystemCoreUtils.GetUniquePath(destPath);
```
Restore 时发生路径冲突，会用 `文件名 (1).ext` 的形式重命名。但 `BackupRestoreUndoAction.ToggleAsync` 将 `_filePath` 更新为新路径：
```csharp
_filePath = await _backupService.RestoreAsync(_record);
_record = null;
```
连续 Undo → Redo → Undo 多次，文件会在不同路径间反复移动，造成管理混乱。

### 3.11 🟢 BackupCleanupService 用 CreationTime 作为清理依据

**严重程度**: 低

**描述**:
```csharp
// BackupCleanupService.cs L93-104
private static DateTime GetCreationTime(string path)
{
    return Directory.Exists(path)
        ? Directory.GetCreationTime(path)
        : File.GetCreationTime(path);
}
```
文件/目录的创建时间在某些场景下不可靠（系统备份还原、文件迁移等）。建议改用 manifest 中记录的 `DeletionTime`。

---

## 四、建议优先级

| 优先级 | 问题 | 建议方案 | 涉及文件 |
|--------|------|----------|----------|
| **P0** | 3.3 Manifest 并发竞争 | `UpdateManifestAsync` 加 `ConcurrentDictionary<string, SemaphoreSlim>` 按 date 做 key 的锁 | `BackupService.cs` |
| **P0** | 3.2 BackupDirectory 配置未生效 | BackupService 构造函数读取 config 中的 `BackupDirectory`，为空才 fallback 到 LocalAppData | `BackupService.cs`, `Bootstrapper.cs` |
| **P1** | 3.1 两个备份目录不统一 | 将 DeleteUndoAction 也改为通过 BackupService 管理，废弃 UndoBackup 目录 | `UndoableActions.cs`, `BackupCleanupService.cs` |
| **P1** | 3.6 保留策略不一致 | 合并 BackupCleanupService 和 BackupService.CleanOldBackupsAsync，共享同一配置源 | `BackupCleanupService.cs`, `BackupService.cs` |
| **P1** | 3.9 UndoService.Clear 未清理 BackupService | Clear() 时也遍历检查 BackupRestoreUndoAction 并标记为可过期 | `UndoService.cs` |
| **P2** | 3.5 清理按钮未实现 | 实现 `BackupSettingsPanel.CleanButton_Click` 调用 BackupService.CleanOldBackupsAsync | `BackupSettingsPanel.xaml.cs` |
| **P2** | 3.4 备份文件名无原始信息 | 文件名改为 `{originalName}_{guid}.ext` | `BackupService.cs` |
| **P2** | 3.8 无总大小限制 | BackupService.CleanOldBackupsAsync 增加总大小参数，从配置读取 | `BackupService.cs` |
| **P2** | 3.7 跨卷删除无进度反馈 | CreateBackupAsync 增加 IProgress 回调参数 | `BackupService.cs`, `FileOperationService.cs` |
| **P3** | 3.10 路径漂泊 | RestoreAsync 路径冲突时不更新 `_filePath`，或增加白名单校验 | `BackupRestoreUndoAction.cs` |
| **P3** | 3.11 CreationTime 不可靠 | 改用 BackupRecord.DeletionTime 作为清理依据 | `BackupCleanupService.cs` |

---

## 五、问题严重程度矩阵

```
严重性     │
          │
   高     │    3.2 (配置失效)    3.3 (并发覆盖)
          │
   中     │    3.1 (双目录)      3.5 (按钮未实现)
          │    3.6 (策略打架)
          │
   低     │    3.7 3.8 3.9 3.10 3.11
          │    3.4
          │
          └──────────────────────────────▶ 影响范围
                   单个模块         全流程
```

---

## 六、关键文件索引

| 文件路径 | 行数 | 说明 |
|----------|------|------|
| `Services/Backup/IBackupService.cs` | 14 | 备份服务接口 |
| `Services/Backup/BackupService.cs` | 246 | 备份服务核心实现 |
| `Services/Backup/BackupRecord.cs` | 21 | `BackupRecord` + `BackupManifest` 数据模型 |
| `Services/FileOperations/Undo/UndoableActions.cs` | 528 | `DeleteUndoAction`、`MoveUndoAction`、`NewFileUndoAction` |
| `Services/FileOperations/Undo/BackupRestoreUndoAction.cs` | 80 | BackupService ↔ Undo 桥接层 |
| `Services/FileOperations/Undo/UndoService.cs` | 166 | 撤销栈管理服务 |
| `Services/FileOperations/Undo/BackupCleanupService.cs` | 126 | UndoBackup 目录清理 |
| `Services/FileOperations/FileOperationService.cs` | 654 | 文件操作统一入口 |
| `Services/FileSystems/FileSystemCoreUtils.cs` | 183 | SafeDeleteFileAsync、SafeDeleteDirectoryAsync |
| `ViewModels/BackupViewModel.cs` | 262 | 备份管理页 ViewModel |
| `Models/Config/UserSettings.cs` | 132 | `BackupSettings` 配置模型 |
| `Controls/Settings/BackupSettingsPanel.xaml.cs` | 122 | 备份设置面板 |

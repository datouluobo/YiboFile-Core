<Window x:Class="YiboFile.Dialogs.LibraryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="库管理" 
        Style="{StaticResource BaseDialogWindowStyle}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight">
    
    <Grid Margin="0" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="库名称:" Foreground="{DynamicResource ForegroundPrimaryBrush}"/>
        <TextBox Grid.Row="1" x:Name="LibraryNameTextBox" Margin="0,5,0,15" Padding="5,3"/>
        
        <Label Grid.Row="2" Content="库路径:" Foreground="{DynamicResource ForegroundPrimaryBrush}"/>
        
        <Grid Grid.Row="3" Margin="0,5,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" x:Name="LibraryPathTextBox" Margin="0,0,10,0" Padding="5,3"/>
            <Button Grid.Column="1" Content="浏览..." Click="BrowseButton_Click" Style="{StaticResource DialogButtonStyle}"/>
        </Grid>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Click="OK_Click" Style="{StaticResource PrimaryDialogButtonStyle}"/>
            <Button Content="取消" Click="Cancel_Click" Style="{StaticResource DialogButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

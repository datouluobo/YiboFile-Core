<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:YiboFile.Controls"
                    xmlns:previews="clr-namespace:YiboFile.ViewModels.Previews"
                    xmlns:behaviors="clr-namespace:YiboFile.Controls.Behaviors">
                    
    <Style x:Key="ToolbarToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentLightBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Previews DataTemplates -->
    <DataTemplate DataType="{x:Type previews:ImagePreviewViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Toolbar -->
            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Dimensions}" Foreground="{DynamicResource ForegroundPrimaryBrush}" VerticalAlignment="Center" Margin="5,0" FontSize="12"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="➕" Command="{Binding ZoomInCommand}" CommandParameter="{Binding ElementName=PreviewImage}" Width="34" Height="34" Margin="2" ToolTip="放大"
                                Style="{DynamicResource IconButton}" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Button Content="➖" Command="{Binding ZoomOutCommand}" CommandParameter="{Binding ElementName=PreviewImage}" Width="34" Height="34" Margin="2" ToolTip="缩小"
                                Style="{DynamicResource IconButton}" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Button Content="1:1" Command="{Binding ResetZoomCommand}" MinWidth="34" Height="34" Padding="5,0" Margin="2" ToolTip="原始大小"
                                Style="{DynamicResource IconButton}" FontSize="12" FontWeight="Bold" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Button Content="⛶" Command="{Binding FitToWindowCommand}" Width="34" Height="34" Margin="2" ToolTip="适应窗口"
                                Style="{DynamicResource IconButton}" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Button Content="↻" Command="{Binding RotateCommand}" Width="34" Height="34" Margin="2" ToolTip="旋转"
                                Style="{DynamicResource IconButton}" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <Button Content="打开" Command="{Binding OpenExternalCommand}" Margin="5,0,0,0" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" PanningMode="Both">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFitToWindow}" Value="True">
                                <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
                                <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>
                <Image x:Name="PreviewImage" Source="{Binding ImageSource}" RenderOptions.BitmapScalingMode="Fant">
                    <Image.LayoutTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="{Binding ZoomLevel}" ScaleY="{Binding ZoomLevel}"/>
                            <RotateTransform Angle="{Binding RotationAngle}"/>
                        </TransformGroup>
                    </Image.LayoutTransform>
                    <Image.Style>
                        <Style TargetType="Image">
                            <Setter Property="Stretch" Value="None"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFitToWindow}" Value="True">
                                    <Setter Property="Stretch" Value="Uniform"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </ScrollViewer>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:MarkdownPreviewViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Toolbar -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundPrimaryBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ToggleButton Content="📄 预览/源码" IsChecked="{Binding IsSourceView}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                    <ToggleButton Content="自动换行" IsChecked="{Binding IsWordWrap}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" Visibility="{Binding IsSourceView, Converter={StaticResource BooleanToVisibilityConverter}}" FontSize="12" VerticalContentAlignment="Center"/>
                    <Button Content="刷新" Command="{Binding ReloadCommand}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                    <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </StackPanel>
                </Grid>
            </Border>

            <controls:MarkdownPreviewControl Grid.Row="1"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:TextPreviewViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Toolbar using standard controls to match TextPreviewToolbar features -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundPrimaryBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding Encoding.EncodingName}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource ForegroundSecondaryBrush}" FontSize="12"/>
                    <ToggleButton Content="自动换行" IsChecked="{Binding IsWordWrap}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                    <ToggleButton Content="编辑" IsChecked="{Binding IsEditMode}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                    <Button Content="保存" Command="{Binding SaveCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                    <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </StackPanel>
                </Grid>
            </Border>

            <TextBox Grid.Row="1" Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" 
                     IsReadOnly="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}" 
                     TextWrapping="{Binding IsWordWrap, Converter={StaticResource BooleanToTextWrappingConverter}}"
                     FontFamily="Consolas" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="Transparent"
                     Foreground="{DynamicResource ForegroundPrimaryBrush}"
                     BorderThickness="0"
                     Padding="5"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:ArchivePreviewViewModel}">
        <ListView ItemsSource="{Binding Entries}" behaviors:ColumnResizeNavigationBlocker.SuppressHeaderNavigation="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="大小" DisplayMemberBinding="{Binding Size}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:ChmPreviewViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ToggleButton Content="目录" IsChecked="{Binding IsTocVisible}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                    <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </StackPanel>
                </Grid>
            </Border>

            <controls:ChmPreviewControl Grid.Row="1"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:WordPreviewViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Toolbar -->
            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="🔄 转换" Command="{Binding ConvertCommand}" Visibility="{Binding NeedsConversion, Converter={StaticResource BooleanToVisibilityConverter}}" IsEnabled="{Binding IsConverting, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="刷新" Command="{Binding ReloadCommand}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <controls:WordPreviewControl/>
                
                <!-- Conversion Overlay -->
                <Border Background="{DynamicResource OverlayBrush}" Visibility="{Binding NeedsConversion, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="⚠️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding ConversionMessage}" FontSize="16" Foreground="{DynamicResource ForegroundPrimaryBrush}" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="400" Margin="0,0,0,20"/>
                        <Button Content="转换为 DOCX 预览" Command="{Binding ConvertCommand}" IsEnabled="{Binding IsConverting, Converter={StaticResource InverseBooleanConverter}}" Padding="20,10" FontSize="16" HorizontalAlignment="Center"/>
                        <ProgressBar IsIndeterminate="True" Height="4" Margin="0,20,0,0" Visibility="{Binding IsConverting, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:MediaPreviewViewModel}">
        <Grid>
            <controls:VideoPreviewControl FilePath="{Binding FilePath}" IsPlaying="{Binding IsPlaying, Mode=TwoWay}">
                <controls:VideoPreviewControl.Style>
                    <Style TargetType="{x:Type controls:VideoPreviewControl}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsVideo}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </controls:VideoPreviewControl.Style>
            </controls:VideoPreviewControl>
            
            <controls:AudioPreviewControl FilePath="{Binding FilePath}">
                <controls:AudioPreviewControl.Style>
                    <Style TargetType="{x:Type controls:AudioPreviewControl}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsVideo}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </controls:AudioPreviewControl.Style>
            </controls:AudioPreviewControl>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:FolderPreviewViewModel}">
        <ListView ItemsSource="{Binding Items}" behaviors:ColumnResizeNavigationBlocker.SuppressHeaderNavigation="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="大小" DisplayMemberBinding="{Binding Size}" Width="70"/>
                    <GridViewColumn Header="修改日期" DisplayMemberBinding="{Binding ModifiedDate}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:DocumentPreviewViewModel}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="📄" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="{Binding FileInfo}" TextAlignment="Center" Foreground="{DynamicResource ForegroundSecondaryBrush}"/>
            <Button Content="使用默认程序打开" Margin="0,20,0,0" Padding="15,5"
                    Command="{Binding OpenExternalCommand}"
                    Style="{DynamicResource PrimaryButtonStyle}"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:PdfPreviewViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Simple Toolbar for PDF -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundPrimaryBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            <controls:PdfView Grid.Row="1" FilePath="{Binding FilePath}"/>
        </Grid>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type previews:HtmlPreviewViewModel}">
        <Grid>
             <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Toolbar -->
            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ToggleButton Content="📄 源码" IsChecked="{Binding IsSourceView}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <ToggleButton Content="编辑" IsChecked="{Binding IsEditMode}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <ToggleButton Content="自动换行" IsChecked="{Binding IsWordWrap}" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="0,0,5,0" Height="34" Padding="10,0" Visibility="{Binding IsSourceView, Converter={StaticResource BooleanToVisibilityConverter}}" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="保存" Command="{Binding SaveCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="刷新" Command="{Binding ReloadCommand}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            <controls:HtmlPreviewControl Grid.Row="1" FilePath="{Binding FilePath}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:ExcelPreviewViewModel}">
        <Grid>
             <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
             <!-- Toolbar -->
            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" PanningMode="HorizontalOnly" MaxWidth="600" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Margin="0,0,0,0">
                        <ListBox ItemsSource="{Binding Sheets}" 
                                 SelectedItem="{Binding SelectedSheet}"
                                 Background="Transparent" BorderThickness="0">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ToggleButton Content="{Binding Name}"
                                                            IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                            Style="{StaticResource ToolbarToggleButtonStyle}"
                                                            Margin="0,0,5,0" Height="34" Padding="12,0"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </ScrollViewer>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                         <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            <controls:ExcelPreviewControl Grid.Row="1"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:CadPreviewViewModel}">
        <Grid>
             <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
             <!-- Toolbar -->
            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{Binding ConvertStatusText}" Command="{Binding ConvertCommand}" Visibility="{Binding IsDwg, Converter={StaticResource BooleanToVisibilityConverter}}" IsEnabled="{Binding IsConverting, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            <controls:CadPreviewControl Grid.Row="1"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:PowerPointPreviewViewModel}">
        <Grid>
             <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
             <!-- Toolbar -->
            <Border Grid.Row="0" Background="Transparent" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="5" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="预览" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource ForegroundSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="·" FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" 
                                   Margin="6,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="13" Foreground="{DynamicResource ForegroundTertiaryBrush}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="250"
                                   Text="{Binding DataContext.SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type controls:PreviewAttachmentControl}}, FallbackValue=''}"
                                   Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="刷新" Command="{Binding ReloadCommand}" Margin="0,0,5,0" Height="34" Padding="10,0" FontSize="12" VerticalContentAlignment="Center"/>
                        <Button Content="打开" Command="{Binding OpenExternalCommand}" Height="34" MinWidth="60" Padding="10,0" FontSize="12" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            <controls:PowerPointPreviewControl Grid.Row="1"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type previews:ErrorPreviewViewModel}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="❌" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding ErrorMessage}" FontSize="16" Foreground="{DynamicResource ForegroundPrimaryBrush}" TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>
